Audit complete — report at `ai-campaign/state/runs/effect-free-adjudication-command-for-hard-crash--65f1a8e7/artifacts/adversarial_review-9c87f3a7/meta/audit-dispatch-adjudicate.md`. Every claim traced to code. Verdict per area:

**1. TOCTOU (`--core-bin-sha256` → `--core-bin`): machinery is sound; design must mandate reuse.** `prepare_binary` (core_terminal_bridge.rs:96) opens once, copies into a sealed memfd, hashes the **sealed memfd**, executes via `/proc/self/fd/N` (pinned_executable.rs:32–52) — bytes executed == bytes hashed == pinned digest, re-verified on every invocation. Gaps: old Cores (pre-`hard_crash`) pass the `--protocol-version` preflight and only fail at decide with a redacted error; 128 MB hash shares the 15 s decide deadline.

**2. Bodies / digest-only Hub: constraint verified, binding enforced, error quality missing.** The v4 claims table persists only digests (claim.rs:121–151; contrast scheduled v23 which stores bodies). Operator bodies are bound to the claim by `validate_claim_against_sources` (validation.rs:171) in Rust **and** Go (`validate_claim.go:44`) — wrong bodies cannot corrupt state. But a wrong body fails deep in the Core with "Core terminal process failed"; design must add a preflight digest cross-check with a distinct refusal, and must **never** reuse `PreparedDispatchDependencies::prepare`, which reads `OPENAI_API_KEY`.

**3. stdin-collision / isolation: pattern exists, new parser is the risk.** Execute rejects `- -` (dispatch_execute_args.rs:97) and consumes stdin before any spawn; Core children are `env_clear` + piped + own process group + kill-on-timeout. The new `parse_dispatch_adjudicate` is unshared code — dual-stdin rejection and read-before-spawn must be specified.

**4. AdjudicationRefused:** zero-mutation is already guaranteed by the single `BEGIN IMMEDIATE` terminalize CAS (`reject_terminal_replay`, `ensure_claim_source`, guarded `transition_run`, exact-DELETE `release_lane`). Gaps: distinct messages per refusal cause (not-stranded / digest mismatch / old-Core / CAS conflict kept separate), explicit exit-code contract (everything is exit 1 today), and do **not** copy execute's blanket Core→`DispatchQuarantined` mapping.

**5. No-credential guarantee (A3): structurally real with two traps.** The send path requires provider+credential fields (execution.rs:60–80); the store trait is persistence-only. Traps: the wiring layer (`execution_service`, `PreparedDispatchDependencies`) reads the key — a new `adjudication_service()` must be specified, and the A3 test should run with a **poisoned** `OPENAI_API_KEY` present (the `CREDENTIAL_SECRET` fixture pattern) proving it's never read.

**Cross-cutting:** `hard_crash` lockstep is feasible — 4 touch points (Rust enum mod.rs:51, Rust `valid_uncertainty` artifact_validation.rs:112, Go `uncertaintyClasses` validate_artifact.go:9, outcome mapping unchanged); both validators accept the all-false no-evidence artifact; A2 is executable with the real pinned Core (`build_go_core`).

Bottom line: no holes in existing machinery — 7 specification gaps the design must carry into implement (listed in the report summary), all blocking at design-gate level because the deliverable is a summary that doesn't state them.
