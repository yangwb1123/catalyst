Done. The amended design is at `ai-campaign/state/runs/effect-free-adjudication-command-for-hard-crash--65f1a8e7/artifacts/design-a77de8a6/task-1-design.md` (388 lines, replacing the 15-line summary — which was the entire prior deliverable and contained none of the 8 mandated items). I re-verified every review claim against the current tree before writing (the design's original line citations had drifted; corrected to actual lines: `service.rs:195-196`/`:209-210`, `dispatch_execute_args.rs:59-60`).

**Verification: all 8 findings incorporated, each with a settled convention.**

1. **Re-entry asymmetry — SETTLED: refuse.** Table of the four existing conventions in §7.1; decision: `AdjudicationRefused::NotStranded`, zero-mutation, exit 1. Grounds: closest analogue is scheduled *adjudicate* (refuses), not scheduled *terminalize* (whose Ok-noop serves a supervisor loop); the v4 store already refuses (`reject_terminal_replay` → Conflict), so an Ok-noop would force swallowing store errors; and silent exit-0 would hide that the operator's bodies/Core were ignored.
2. **Exit-code contract — SETTLED.** 0 = adjudicated, 1 = every refusal + store error (uniform `ExitCode::FAILURE`; refusal-vs-failure is message-only, stable `adjudication refused:` stderr prefix; a bespoke refusal code rejected for consistency), 2 = parse errors. Four distinct `AdjudicationRefused` causes with distinct messages: `NotStranded` / `DigestMismatch` / `CoreRefused` (old-Core at decide, re-pin hint) / `CasConflict` (retryable). Explicit prohibition of execute's blanket Core→`DispatchQuarantined` copy (§7.2).
3. **Digest preflight — MANDATED.** Decode + canonicalize bodies, compare `authorization_sha256`/`pricing_snapshot_sha256` vs persisted claim before bridge construction → `DigestMismatch`; bridge reuse mandated (`PinnedCoreTerminalBridge::new` + `decide_json` unchanged, no hash-then-run-by-path); `PreparedDispatchDependencies::prepare` (reads `OPENAI_API_KEY`) prohibited; new `adjudication_service()` wiring is store/codec/bridge/metadata only (§7.3).
4. **stdin — SPECIFIED.** `parse_dispatch_adjudicate` rejects `- -` (new code, execute's check is unshared); ordering: stranded guard → stdin to EOF bounded → bridge preflight spawn → decide; no spawn before stdin consumed (§7.4).
5. **Failure mode #1 extended** with the busy-timeout branch: 5 s → `SQLITE_BUSY` → `Unavailable` → store error with contention message, retryable — explicitly not `DispatchQuarantined` (§7.5).
6. **`ensure_persisted` localized** — mandatory explicit assertions (status, run.v==5, last_event_seq==5, journal_bytes delta, event count) plus doc-comment naming the transitive readback chain it doubles (§7.6).
7. **Acceptance list updated** — new A-hardcrash (extend the 11-class list to 12 in the same change; `matches!` is non-exhaustive so the feature would otherwise be silently dead) and A-scheduled-fence (scheduled validator still rejects `HardCrash`) (§7.7, §9).
8. **Idempotence test strengthened** — re-invocation with *different* bodies must return the byte-identical stored inspection (status/run_version/last_event_seq/journal_bytes/digests unchanged, no new events) (§7.8, A5).

Plus the other blocking review items folded in: reuse-not-reverify TOCTOU, old-Core re-pin REQUIRED, clock-skew refusal cause, 128 MB/15 s deadline interplay, signal-orphan residual, poisoned-`OPENAI_API_KEY` A3 test (a) absent + (b) poisoned → byte-identical output, and the CLI slots (`parse_run_dispatch` "adjudicate", `dispatch_claim_key_error`/`accepts_idempotency_key` treat Adjudicate like Execute, `cli_usage.rs` dead-end-sentence update). Failure modes expanded 10→11; §8/§10 cover migration and accepted residuals.
