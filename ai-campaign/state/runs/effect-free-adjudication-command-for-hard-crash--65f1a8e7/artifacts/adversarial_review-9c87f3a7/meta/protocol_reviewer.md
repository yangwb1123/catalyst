Verification complete. Report saved to `ai-campaign/state/runs/effect-free-adjudication-command-for-hard-crash--65f1a8e7/artifacts/adversarial_review-9c87f3a7/verify-cross-language-invariants.md`. All five invariants verified against the codebase; two targeted test runs executed (Go graphterminal outcome-mapping suite `ok`; Rust closed-world classification test `ok`).

**1. `hard_crash` / `uncertaintyClasses` lockstep — VERIFIED (additive, fail-closed both ways)**
`hard_crash` exists in neither language yet. Base lockstep is exact: Go map `validate_artifact.go:9-14` (11 classes) == Rust enum `GroupAgentNodeTerminalClassification` (`mod.rs:51-68`, snake_case) 1:1; both validators use the identical chronology/missing-usage formula, and both outcome mappings are already class-agnostic (`uncertainty → failed_uncertain`: `receipt.go:96-98`, Rust `(Uncertainty, _) => FailedUncertain`). Touch surface is minimal: 1 map entry + 1 enum variant + 1 `matches!` arm. **Finding:** the Rust side is *test-enforced, not compile-enforced* — `matches!` is non-exhaustive, so adding the variant silently fails closed until `every_fixed_uncertainty_class_is_closed_world_and_nonretryable` (`domain/tests/group_agent_node_lifecycle.rs:181`, explicit 11-class list) is extended. Asymmetry is safe in both directions (Go-only → Core rejects; Rust-only → Hub rejects; both loud, zero-mutation). The shared enum also means the scheduled-family validator keeps rejecting `hard_crash` — correct, since ADR-0034's pid-sidecar adjudication owns that family.

**2. `transition_run` CAS — VERIFIED EXACT** (`sqlite_hub/group_agent_node_lifecycle/terminalize.rs:214-253`): single BEGIN IMMEDIATE txn; `UPDATE ... WHERE run_version=4 AND status='dispatch_unknown' AND execution_contract_present=1 AND dispatch_request_present=1 AND dispatch_authority_released=1 AND last_event_seq=?4 AND EXISTS(...seq=?4 AND event_sha256=?5)` with ?4 = `expected_last_event_seq` (hardcoded 4 in **both** Go `receipt.go:73`/`:87` and Rust `terminal_validation.rs:178`) and ?5 = claim-head digest. Schema conjunct matches `v12_sql.rs:88-95` exactly; `changed==1` else conflict.

**3. `release_lane` — VERIFIED EXACT** (`:255-283`): 5-key DELETE (lane_ownership_id, graph_run_id, dispatch_id, project_lane_sha256, claim_event_sha256), `changed==1` else conflict; inside the same txn as the run transition, so failure rolls back everything.

**4. Replay/re-adjudication zero-mutation — VERIFIED**: `reject_terminal_replay` (artifact/receipt present ⇒ "already terminal") runs before the *first* write (`insert_artifact`), in an erroring txn — unconditional refusal, byte-identity irrelevant. Execute re-entry is idempotent via `existing_lifecycle` → `AlreadyClaimed` (`service.rs:130-150`).

**5. Protocol v1 + old-Core failure modes — VERIFIED SOUND AND COMPLETE**: bridge handshake (`core_terminal_bridge.rs:76-85`) requires `graph-node-terminal-receipt --protocol-version` → `"1"` (`command.go:50-51`). Old Core without the subcommand → stderr usage + nonzero exit → loud bridge failure; old Core with subcommand but pre-`hard_crash` → `BuildReceipt` `errInvalidControl` → loud. Both pre-mutation (bridge is pure; no store handle). Version 1 staying is sound because the handshake covers the envelope, and vocabulary growth is fail-closed; v1 is also schema- and constant-enforced on both sides. Residual risk (no v4 pid sidecar) matches ADR-0034's explicit v4-family fence.

Also confirmed the design's other load-bearing claims: Hub persists only claim *digests* (`claim.rs:113-160`, no authz/pricing bodies — hence the `--authorization --pricing` flags), schema v23 current with the scheduled `'adjudicated'` status, and the `strand_v4_with_local_provider` fixture exists as the A2 basis.

**Overall: PASS** — no blockers; the only required hardening is adding the Rust closed-world test extension to the acceptance list (Finding 1).
