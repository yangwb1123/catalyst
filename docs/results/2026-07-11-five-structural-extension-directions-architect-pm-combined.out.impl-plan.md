文档已完成。以下是其结构摘要：

---

## 产出：Tech Lead 分析报告

**文件**: `docs/analysis/2026-07-12-tech-lead-analysis-five-structural-directions.md`（1053 行，~38KB）

### 报告结构

| 章节 | 内容 |
|------|------|
| **1. 执行摘要** | 优先级排序（P0×2·P1×2·P2×1）、交叉验证发现修正表、工程红线背景 |
| **2. 任务分解** | **20 个任务**，拆分为 2-4 小时粒度，含精确的验收标准 |
| **3. 执行顺序与依赖图** | Phases A/B/C 三阶段 + Mermaid 依赖图 + 并行策略表 |
| **4. 技术风险** | 6 项风险 + 外部依赖 + 性能考虑 + 测试难点 |
| **5. 资源评估** | 人员需求 + 5 个里程碑 + 阻塞点解决策略 |
| **6. 质量保证** | 覆盖要求 + 集成策略 + 审查要点 + 性能基准 |
| **7. 实施计划** | Mermaid 甘特图 + 阶段详情 + 工时汇总（~14.3 天开发） |
| **8. 附录** | 6 个 ADR（含所有关键架构决策） |

### 核心设计决策

所有 5 个交叉验证修正点已纳入任务设计：
1. **方向三 v1** 窄化为 Go `import` 路径检测（T001-T003），复用 `arch-check` 已有模式
2. **方向五** 合约直接嵌入 agent 卡 frontmatter（T005-T009），零额外文件
3. **方向一** 先做数据密度检测（T011）再出校准报告（T012），样本不足时静默跳过
4. **方向二** 冷启动基线编译进 binary（T015），离线可用
5. **方向四** 前导 T010（运行边界分隔行）是所有 trace 消费者的前置条件

### 建议的启动步骤

1. `forge validate` 确认当前 `cmd/forge` 包文件数预算余量
2. 并行启动 T001（模式表）+ T005（合约 Schema）+ T010（边界分隔）——三个任务零依赖
3. 预计 Day 5 完成 Phase A 全部 10 个任务，Day 12 完成 Phase B
