分析已写入 `docs/requirements/2026-07-12-tech-lead-analysis-five-directions.md`（574 行，约 25KB）。

## 产出概要

**7 个章节 + 1 个附录**：

### 1. 任务分解（30 个 task）
- 方向① 可观测性 API：6 task（`OBS-001` ~ `OBS-006`）~20h
- 方向② 跨会话编排：6 task（`CRS-001` ~ `CRS-006`）~23h
- 方向③ 插件化：6 task（`PLG-001` ~ `PLG-006`）~19h
- 方向④ 错误语义：6 task（`ERR-001` ~ `ERR-006`）~18h
- 方向⑤ 自监控：6 task（`SELF-001` ~ `SELF-006`）~19h

每个 task 均包含：文件路径、前置依赖、工时、验收标准。

### 2. 执行顺序
Mermaid 依赖图 + 6 个可并行 task 组表。明确 **方向①是所有方向的先决条件**，但方向⑤的可交付价值最快（不需要改事件格式，直接在 trace 上做检测）。

### 3. 技术风险（25 条已识别风险）
每个方向 5 条风险 × 概率/影响/缓解策略。最高风险项包括：
- Windows socket 兼容性（抽象 Listener 接口）
- TUI 连接时事件回放（先发 checkpoint 快照）
- Frontmatter YAML 解析 vs 零依赖原则（推荐 JSON frontmatter）

### 4. 资源评估
- 最小可行团队 3 人（2 Go + 1 TUI）
- 6 个里程碑（M1-M6）
- 每个方向的零回退成本机制（全部 additive）

### 5. 质量保证
- 每个新包的单元测试覆盖率 ≥75-85%
- 6 个关键集成测试场景
- 5 个方向 × 代码审查要点表
- 5 个性能测试场景 + 基线/目标

### 6. 实施计划
4 阶段甘特图：
- **Sprint N**（Jul 14-25）：基础设施 → 方向①全 + 方向⑤全
- **Sprint N+1**（Jul 28-Aug 8）：核心功能 → 方向④全 + 方向②③起步
- **Sprint N+2**（Aug 11-22）：集成调优 → 方向②③完成 + 性能测试
- **Sprint N+3**（Aug 25-Sep 5）：发布准备 → 迁移工具 + 文档

### 7. 附录：代码库真实差异
记录了 4 处原始分析文档与代码库实际不符之处（`PhaseType` 枚举不存在、`BuildPlan` 不存在等），并说明每处差异对规划的影响。
