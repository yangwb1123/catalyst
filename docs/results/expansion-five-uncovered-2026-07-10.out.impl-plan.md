分析报告已生成，写入 `docs/tech-lead/2026-07-12-five-runtime-blindspots-tech-lead-analysis.md`（501 行）。

---

## 核心结论摘要

### 优先级重排（基于审查反馈校准）

| 方向 | 调整后优先级 | 核心理由 |
|------|-------------|---------|
| **方向二 · 补偿撤销** | **P0** | 真点火已验证「错误迭代不可逆」是活跃风险；审查确认所有事实核准，信任度最高 |
| **方向一 · 确定性回放** | **P1** | 审计合规基线，但 FullTrace 存储成本需 R-006 预算控制 |
| **方向三 · 故障隔离** | **P1** | Memory 全局共享是活跃风险（非未来问题）；审查确认 lock order 只防死锁不防故障传播 |
| **方向五 · 渐变安全** | **P1** | `BudgetAdjustTier` 已有基础设施，warn+block 两档最简实现成本低、收益高 |
| **方向四 · 信任加权记忆** | **P2** | 「零消费」修正为「展示层消费已存在」，Query 层信任加权是真正的增量 |

### 关键整合点

审查反馈的 6 项修正已全部整合到任务分解中：
- **F1** → 方向一重放语义简化（memory.Append 的 CreatedAtUnix 已由 caller 注入）
- **F2** → 方向四起点从「零消费」改为「增强现有展示层→查询层加权」
- **F3/F6** → 方向五引用 `BudgetAdjustTier` 作为已有基础，非零构建
- **F5** → 方向一复用 `replay_test.go` 既有 fixture

### 实施规模

| 维度 | 数据 |
|------|------|
| **总任务数** | 34 个（2-5h/个） |
| **预估总工时** | ~116h（不含缓冲） |
| **实施周期** | 12 周（含 10 天缓冲） |
| **团队规模** | 5-6 人（4 核心 + 1 QA + 1 TL） |
| **总代码增量** | 预估 ~3500-4500 行 Go（含测试） |
| **红线合规** | 全部新文件 ≤500 行，函数 ≤50 行，零外部依赖 |
