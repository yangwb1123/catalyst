There are many architecture documents (`.arch.md`) and implementation plans (`.impl-plan.md`) in `docs/results/`. However, you haven't specified **which** task to implement.

Could you please tell me:

1. **Which architecture document and implementation plan** should I follow? (e.g., a specific path from `docs/results/`)
2. **What specific feature or component** needs to be implemented?
3. **Any additional context** about priority or scope?

Alternatively, if you want me to read a specific file from `docs/results/` to get started, just point me to it. I'm ready to dive in once I have the target.
