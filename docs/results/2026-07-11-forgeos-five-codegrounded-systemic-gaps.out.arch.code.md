I see you've shared the **Code Implementer role prompt** from `ai-dev/prompts/code-implementer.md`. However, to actually perform code implementation, I need the task-specific input documents.

**I'm ready to implement**, but I need you to provide:

1. **Architecture Design Document** (`.arch.md`) — the technical architecture
2. **Implementation Plan Document** (`.impl-plan.md`) — the task breakdown
3. **Raw Requirements** — the original feature/bug description

You can provide these by:
- **Attaching the file paths** from the workspace (e.g., `docs/results/*.arch.md` + `docs/results/*.impl-plan.md`)
- **Pasting the content** directly
- **Telling me which requirement** from `docs/requirements/` to process

I see several existing `.arch.md` and `.impl-plan.md` files under `docs/results/`. If you want me to pick one of those, please tell me which one. Or if this is part of a pipeline execution, share the requirement document path.

What would you like me to implement?
