# ForgeOS Functional Requirements Audit

**Methodology and authority boundary.** This audit is derived only from ForgeOS's adopted
commitments: root `ROADMAP.md`, `.agent/{ROADMAP,PROJECT,ARCHITECTURE}.md`, and all adopted ADR
records while preserving their actual Accepted / Proposed / Superseded status,
`.agent/DECISIONS.md`, shipped workflows, and the current role/Skill adapter registry, plus the
implementation/verification record in `.agent/CURRENT_SPRINT.md`. It is not an external
specification. In particular, `FUNCTIONAL_REQUIREMENTS_BACKLOG_SCAN.md` explicitly labels its
413/431 generated ideas as *unadopted*; their internal P0/P1 labels are discovery signals, not
requirements, unless a later authoritative source or Accepted ADR adopts one.

The large tables below preserve the original 2026-07-02 five-pass audit as a historical
baseline. Its then-current counts and source references must not be read as a present-tense
inventory. A fresh independent audit was run on 2026-07-27 against the current working tree,
including Go build/vet/test/race, Rust test/clippy/check/build, the Node and Python harness
suites, scaffold adversarial tests, and the composed Stop gate. Current deltas are recorded in
the live DONE/DEFERRED rows and `.agent/CURRENT_SPRINT.md`.

### Current closure matrix (2026-08-12)

This matrix is the present-tense closure record for the adopted plan. `DONE` means an
implemented, executable contract with positive and negative tests; it does not mean an
unadopted idea from the backlog scan was silently promoted into scope. `NON-GOAL`
means an Accepted ADR deliberately places that action outside ForgeOS's trust
boundary. `ADOPTED-PLANNED` means an Accepted ADR has made the item an explicit
roadmap commitment, but no executable implementation may be inferred from the
planning artifact. `ADOPTED-STAGED` means candidate contract/code and focused
tests exist, but independent review and final acceptance still gate shipment.

| Status | Adopted requirement | Verifiable delivery |
|---|---|---|
| DONE | Local digest-bound Agent output, review and positive approval (ADR 0064) | All seven canonical command-mode workflows now select `local_digest_v1`; accepted output is published only after exact transport/semantic/artifact/postflight validation and durable claimed receipt. Caller-declared L3/L4 Build uses challenge-bound `reviewer_v2` with QA/stage/chain freshness, Design/Deploy/Rollback positive approval requires current receipt-bound ApprovalContext/v3 marker/v2 release receipt, and checkpoint/chain v5 restores exact pinned outputs without legacy downgrade. Receipt/context/marker remain same-UID observations/control references, not ApprovalRecord, identity, SoD, signed PDP/Grant, semantic truth or effect authority. |
| DONE | Authority-free GraphSnapshot v1 foundation (ADR 0065) | The generic v1 identity/taxonomy/provenance/coverage shape and one exact ADR-0053 selected-module lexical profile are delivered through byte-identical Go/Python pure projectors, explicit-input CLI, strict full-reconstruction checker, golden, registry and universal scaffold. Every nonlocal dependency and diagnostic is preserved, coverage is PARTIAL, system/freshness are UNKNOWN, and the result performs no live capture or selected-build/cross-surface analysis and supplies no truth, authority, Impact/Cost/Risk, G3 or Assessment Join. |
| DONE | Local Go lexical test-source GraphSnapshot profile (ADR 0066) | A dedicated request/envelope API projects one test source-set node for each ADR-0053 package with nonempty `test_files` plus one module→test structural edge, while preserving ADR 0065 API/Schema/golden bytes and stable legacy IDs. Go/Python pure projectors, explicit CLI dispatch, strict full reconstruction, a second golden, Skill and universal fresh/legacy scaffold are wired, with its entry retained in current registry v22. Go/test coverage is an exact disjoint PARTIAL partition and system/freshness remain UNKNOWN; lexical source presence is not test discovery, execution, PASS/FAIL, coverage, verified-subject mapping, G3, Assessment Join, Impact/Cost/Risk, persistence or authority. |
| DONE | Proposed-only ArchitectureDecisionRecord v2 (ADR 0067) | New Proposed ADR documents now have a closed exact canonical-JSON frontmatter, fixed nonempty Markdown body sections, declared owner/approver/Claim/Evidence/affected-node/validation/revisit fields and domain-separated body/self digests. Python strict validation, current-`writes_adr` Go candidate validation, a physical golden, registry v22, shadow detector, Skill and fresh/legacy scaffold are wired. Go retains the existing ADR baseline-integrity snapshot, but legacy documents are not parsed or retro-validated as v2 and are not migrated. The declarations are not authenticated or resolved, and this slice supplies no ApprovalRecord, acceptance, graph coverage, compliance, immutable/supersession state machine, persistence, lifecycle or effect authority. |
| DONE | Local Go Package ImpactPreScan v1 (ADR 0062) | The pure local Go evaluator and CLI consume exact canonical ADR-0053 graph bytes plus sorted changed paths, recompute graph/request/node/edge/report/envelope identities, partition seeds, and return the exact reverse local-package fixed point, complete induced local edges and deterministic shortest witnesses. A strict Python checker and exact golden independently reconstruct the same report; scaffold and governance wiring ship the universal checker. Any unresolved path, diagnostic or ambiguous/unresolved/nested/unsupported dependency keeps package closure UNKNOWN, and system impact is always UNKNOWN. This slice performs no live capture or repository/process/network/database/provider access and does not claim selected build, System Knowledge Graph, final ChangeImpact/Cost/Risk, Assessment, truth, authority, completion, persistence or effect. |
| DONE | Caller-declared L3/L4 Build Reviewer strict verdict v1 (ADR 0063) | A Build workflow can select `reviewer_v1`; for caller-declared L3/L4 the runtime requires exactly one safe readonly/fresh Reviewer before QA, overrides mode skip, accepts only an exact final non-empty verdict from a successful executor payload, and rejects skip-ahead, parallel, malformed/missing output and incompatible resume/chain downgrade before release to QA. L0–L2 and `materiality_not_bound` retain the legacy advisory/fail-open path. Materiality is neither inferred nor authenticated; reviewer/model/provider identity, review quality, cryptographic SoD, source/context/policy/artifact digest binding and same-UID state tamper resistance remain unavailable. Old runtimes must reject the unknown contract. Scaffold/upgrade copy only ADR/workflow/role assets and do not install a compatible host runtime. |
| DONE | Local Go package dependency graph observation producer v1 (ADR 0053) | The delivered explicit opt-in Unix-local producer reuses the exact `git-worktree-source-tree-v1` bounded-interval source profile and applies the Go standard-library parser to a `selected-module-all-regular-go-files-union-v1` view. Its graph distinguishes package/module nodes, compile/test dependency edges, external/unresolved references, source coverage and diagnostics. It does not run `go list`, build, test or module commands; resolve actual GOOS/GOARCH/build tags, module-cache/workspace replacements or compiler reachability; prove graph completeness, architecture or change impact; create Evidence/Claim/Atom/Context/Grant/Impact/Cost/Risk; persist data; or attest completion, truth, authority or effects. Schema, exact golden, strict Python checker, Go producer, scaffold/governance wiring, adversarial/resource tests and two independent CLEAN reviews are wired. |
| DONE | Local Evolve locator observation producer v1 (ADR 0052) | The delivered explicit opt-in Unix producer binds the complete canonical `EVOLVE_SCAN_V1: ` report preimage, the shared `git-worktree-source-tree-v1` bounded-interval non-atomic source observation, and zero-or-more exact locators in canonical report order while preserving the same path across distinct relations. Fixed read-only Git subprocesses neither authenticate the Git binary nor prove sandbox, egress or effect containment. The result `CAPTURED_LOCAL_EVOLVE_LOCATOR_SET` attests no scan judgment, completion, truth, authority, Claim, Atom, persistence or effect, and does not automatically invoke ADR 0050. Schema, exact golden, strict bounded Python checker, Go producer, scaffold/governance wiring, adversarial tests and independent CLEAN reviews are wired. |
| DONE | Local gate command observation producer v1 (ADR 0051) | The delivered explicit opt-in Unix producer fixes four exact local gate/check/accept/probe-all commands, canonical Git-root cwd, the actual scrubbed child environment, PATH-resolved top-level executable/symlink/file bytes, bounded-interval Git inventory+entry source observation, raw streams and termination in strict `forgeos.governance.local-gate-command-observation-production/v1`. Default gate APIs remain capture-disabled and legacy-compatible; source capture is not an atomic snapshot or execution pin, and the producer provides no sandbox/effect containment. Go runtime, Python strict checker, exact golden, scaffold/upgrade, race/adversarial suites, full acceptance and two independent CLEAN reviews are wired. Its only result is `OBSERVED_LOCAL_PROCESS`; it attests no PASS, criterion, completion, truth, authority, identity, persistence, external effect or remote execution. |
| DONE | Command observation → Gate/Test EvidenceRecord shadow adapter v1 (ADR 0049) | A strict bounded pure adapter accepts exact compact canonical `forgeos.command-observation/v1` plus Governance binding, domain-separates command, observation, request and existing Evidence identities, and maps only a real `exited` termination into `gate_result|test_run`. Python, Go and Rust implementations plus the Universal checker reproduce exact command/observation/request/Evidence bytes and four digests from one golden; timeout/cancel remain honest non-projectable observations, while sentinels, signal/spawn guesses and output drift fail closed. Its only positive result is `ADAPTED_SHADOW (observation mapping only; no execution, pass, completion, truth, authority, claim, atom, persistence, or effect attestation)`: it does not execute commands, verify stream preimages or producer/digest profiles, interpret exit zero as PASS, append the journal, write persistence or execute effects. SQLite remains v25 with no migration/backfill; real command producer wiring remains separate, while the Evolve locator adapter is delivered independently by ADR 0050. |
| DONE | Artifact provenance → EvidenceRecord shadow adapter v1 (ADR 0048) | A strict bounded pure adapter accepts only exact compact canonical `forgeos.governance.artifact-evidence-adapter/v1` requests containing an explicit eleven-field `forgeos.artifact.v1` record plus Governance binding, then domain-separates canonical source and request identity and emits one existing strict `EvidenceRecord` v1. Python, Go and Rust implementations plus the Universal checker reproduce the same source/request/Evidence bytes and digests from one golden and reject legacy format, duplicate/unknown/noncanonical/Unicode/path/time/list/hash/identity/output drift. Its only positive result is `ADAPTED_SHADOW (no truth, authority, claim, atom, persistence, or effect attestation)`; it does not read the current artifact file, authenticate manifest/agent/model/collector, create Claim/Atom, append the journal, write any persistence or execute effects. SQLite remains v25 with no migration or backfill. |
| DONE | Shadow CognitiveAtom Projection v1 (ADR 0047) | A strict pure projector deterministically maps only `fact`, `constraint`, `decision`, `inference`, `assumption`, `hypothesis` and `unknown` shadow KnowledgeClaims into exact CognitiveAtom/source-closure/set bytes and identities across Python, Go and Rust; `lesson` and `proposal` may remain closure members but produce no Atom. Every result is `PROJECTED_SHADOW` with `hardness:none`, null authority and instructions disabled. The contract does not compile prompt/model output, attest truth or authority, satisfy guards, transition lifecycle, execute effects, claim completion or write any journal/database; it is not the complete Kernel ABI. |
| DONE | Canonical Evidence/Claim shadow kernel (ADR 0045) | `EvidenceRecord` and `KnowledgeClaim` v1 now have a strict bounded schema, distinct immutable/logical/version identities, kind-separated canonical SHA-256, closed state matrices and an authority-free shadow admissibility floor. Go, Rust and zero-dependency Python implementations consume one exact UTF-8 golden and reject duplicate/unknown fields, floats, forbidden Unicode, non-canonical bytes, wrong digests, dangling/conflicting references, illegal supersession and authoritative states. The checker and assets are inherited by init/upgrade, but do not authenticate producers, confirm facts, accept decisions, satisfy hard gates, write Hub/knowledge, import Memory/ADR or implement Context/Grant/Approval/Transition. |
| DONE | Local GovernanceRecordJournal v1 (ADR 0046) | Canonical SQLite v25 introduced the journal tables; merged current SQLite v27 retains `forge-runtime governance journal append/show/list/head` and implements atomic local append of 1–256 exact canonical Evidence/Claim records under a bounded caller idempotency key, with domain-separated request/batch identity, immutable record ownership, contiguous structural predecessors, `stored|exact_replay` receipts, metadata-first inspection, explicit content reveal and a rebuildable `structural_sequence_only` head. Deterministic rebuild; mutation-only convergence from v24, canonical journal v25, historical endpoint-only v25, or v26 to v27; current-v27 read-only behavior; old-binary rejection; replay/conflict/corruption/concurrency handling; and scaffold/upgrade compatibility are covered by tests. Reference-closure limits remain resource-exhaustion controls only; scaffold does not install `forge-runtime`, and the journal receipt/head itself supplies no truth, freshness, conflict arbitration, lifecycle, authority or knowledge adoption; ADR 0054's separate projection has its own narrower semantics. |
| DONE | Business-bound UI geometry shadow contract (ADR 0044) | The existing AFDS gains a supporting `ui-geometry` Skill without creating a fourth capability owner or parallel package. Applicable layouts attach a strict, bounded, digest-bound composition source artifact that traces existing actor/flow/action/state/data meaning into regions, axes, groups, semantic spacing, strokes, shape families, responsive dispositions and load-bearing elements. Capture cases may bind raw geometry observations through an exact source/build/fixture/environment report; required failures cannot be hidden by a score. The validator and scaffold/legacy-upgrade tests are executable, while provenance remains declarative and no trusted browser/native Runner, visual judgement or completion authority is claimed |
| DONE | Frontend code architecture governance shadow contract (ADR 0043) | A focused procedural Skill and byte-pinned policy preserve existing capability ownership while defining cohesion, module/public API, state/effect ownership, shared admission, change radius and over-architecture decisions. A strict project JSON contract declares targets, module ownership, layer dependency matrices, public/test entrypoints, budgets, exact baselines and independently approved expiring waivers. The standalone `frontend.code_architecture` detector uses the project TypeScript Compiler API for aliases, extensionless/index resolution and AST-derived metrics; deterministic direction/ownership/cycle/config rules cannot be baselined or waived, while God-file and budget signals remain review-only. Empty targets are N/A, configured Vue/Dart targets are inconclusive, and the detector remains shadow/non-load-bearing with no completion authority |
| DONE | Frontend design decision shadow contract (ADR 0042) | A byte-pinned AFDS policy routes 20 frontend risk triggers through L1–L4/W1–W3 floors and 14 decisions spanning business task, IA, flow/state/action/permission, Profile×Pattern, tokens/components, responsive/input, accessibility, motion/performance, framework state and visual/interaction evidence. Twelve product Profiles and fourteen page Patterns remain orthogonal; three canonical Skill adapters and the user-experience Context route avoid a parallel Agent taxonomy. FrontendDesignPackage separates facts/assumptions, artifacts/proof claims and readiness, rejects completion fields, malformed nested input, evidence reuse and invalid PNG/capture bindings, and ships through `forge check`, init and legacy upgrade. The detector remains `shadow` and non-load-bearing: digests prove bytes, not trusted tool execution or Reviewer identity, and no automatic diff compiler or pre-code completion authority is claimed |
| DONE | Backend engineering decision shadow contract (ADR 0041) | A canonical policy now routes 16 backend/domain/data/contract/reliability/security/performance triggers through per-trigger L1–L4/W1–W3 floors, 14 explicit decision dimensions, risk-conditioned model roles and a persistence-first design gate. Ten dense Skill adapters, a no-self-approval BackendDecisionPackage with byte-pinned policy/Schema, bounded file-resolved evidence, proof type/class/subject checks, readiness dependencies and resolved ADR/Reviewer controls, strict instance validator, security/data/backend Context routes, adversarial tests and init/legacy-upgrade inheritance are present. Whole-tree/context provenance and trusted producer/reviewer attestation remain declarative until runtime; the detector is honestly `shadow` and non-load-bearing, so no automatic diff-to-package compiler or pre-code block is claimed |
| DONE | Machine-readable Agent Engineering governance shadow slice (ADR 0040) | `.agent/engineering` carries a legacy-safe activation contract, exact 14-discipline registry, 17 atomic rules (including backend, frontend-architecture and Evidence/Claim honesty rules), a registry of detectors actually invoked as load-bearing `forge accept` probes, typed bounded Context routes and a monotonic W0–W3 overlay. TaskEvidencePackage forbids writable completion verdicts and validates source-bound structured observations. `harness/check.py` runs the aggregate as check 13; malformed YAML, escaping refs, fake detector bindings, Context/profile downgrades and capability ownership drift fail closed. Forge-init and legacy forge-upgrade inherit the slice; activation remains `shadow`, runtime context routing is not claimed, and only `forge accept` decides completion |
| DONE | Gate freshness and honest convergence | Required gates run at the point of use; stale/N/A results cannot satisfy required or production criteria; unknown stop metrics fail closed |
| DONE | `mode × lifecycle` is one executable policy | Run/evolve/chain restore the effective policy, enforce review/build/action halts, gate floors, depth and model safety floors; Evolve content depth and agent-write authority are independent canonical fields, lifecycle tightening never grants auto-act, and proposal-only stops at a validated `effect: mutate` boundary with no host gate runner; every chain entry and resumable path reconstruction disables repository YAML shims, with option conflicts rejected before reconstruction; resumable checkpoint v3 requires every non-null recovery/resource field, rejects invalid cursor/counter/cap combinations, hashes the full normalized workflow, and binds mode/resolved-lifecycle before trace or Agent startup |
| DONE | Evidence-backed Evolve scan contract (ADR 0020) | The unique first Evolve phase freezes effective depth into one strict evidence-bearing `EVOLVE_SCAN_V1` report. Thorough covers all six dimensions and derives candidate tasks, opportunistic permits only direct-evidence obvious opportunities, and standard/advisory retain honest limits. Full canonical feed-forward is atomic; serial checkpoint v3 restores the report and integer resource envelope without replay, while native parallel promises only iteration-boundary recovery and legacy workflows remain unchanged |
| DONE | Durable lifecycle chain | `forge run --chain` persists a versioned run/stage state across Discover→Design→Review→Build→Deploy→Evolve; resume, cycle/max-stage limits, rejection rework, approval waits and terminal failure reasons have E2E coverage; retained Evolve checkpoint history is copied before the final atomic current commit rather than moving current, and fault tests prove failed history/final commits leave the old current loadable |
| DONE | Durable lifecycle promotion migration (ADR 0016) | Explicit persistent production promotion is dry by default; apply uses a shared lock, canonical intent, exact before/after images, independent receipts and deterministic roll-forward. Explorer promotion composes the declared engineering remediation once, other known modes preserve ROADMAP bytes, transient run/evolve selectors never write, and terminal receipt/provenance drift blocks execution before workflow or Agent side effects |
| DONE | Machine task/output/verdict contracts | Workflow phase identities and normalized per-phase emit targets are unambiguous; Planner `TASK_LIST` is strict; ordinary declared emits must be in-repo regular non-empty files and are provenance-recorded; reviewer/CTO tokens retain their documented advisory routing, while Build QA must declare `qa_v1` and emit an exact final `QA_VERDICT` whose absence, malformation, rejected-loop failure or exhausted repair budget fails closed; ADR and release artifacts add current-attempt freshness, and release validation retains its independent exact verdict/receipt contract |
| DONE | Approval and rejection are auditable state | Private versioned markers, failure-retained/success-consumed rejection, durable approval, conflicting-marker fail-close and release-specific source/artifact binding are tested |
| DONE | Artifact provenance and trace query | Append-only records bind run/workflow/phase/agent/model/path/content/prompt hashes; `forge trace` reads filtered versioned/redacted state and malformed/unknown generations fail safely |
| DONE | Command executor and control-state safety | Prompt uses stdin; ordinary child env is minimal/explicit while release/proposal-only execution removes host config/temp/XDG state, fixes `PATH`, and admits only direct auth plus locale/TLS inputs; output/time/call/depth/budget limits and process-group termination are enforced; `.forge` uses real private directories, bounded regular leaves and atomic unique temps, with Unix no-follow/single-link enforcement; Linux's verified-host-Git boundary rejects tracked `.forge/**` and its portable case/slash aliases before control-state use, and rejects a `--root` nested below the true Git toplevel (non-Unix retains type/static-symlink/identity/permission/size checks but does not claim link-count or adversarial-index provenance); unknown/incomplete sandbox requests fail closed, while explicit Docker and Firecracker runners are wired with stdin, output, timeout and memory bounds; emitted state is redacted/private |
| DONE | Declarative deploy/rollback boundary (ADR 0005) | `release-engineer` is stage-confined to immutable Deploy/Rollback; exact `Edit(<phase.emit>)`, fixed minimal prompt, operator-pinned bytes (not vendor identity), a sealed executable Linux memfd whose final immutable digest/ELF is verified before read-only FD execution (scripts/binfmt rejected; unsupported host policy fails closed), fixed/minimal-env host Git with repository fsmonitor/hooks disabled for source inventory, exact Git-toplevel root binding and portable protected-path alias rejection, whole-tree postflight, strict JSON/verdict, validation feedback, receipt freshness against product state/current stage's fixed artifact set, and human marker contracts; no remote action or credential access |
| DONE | Recursive polyglot acceptance | Go, Node, Python, Rust and Java project discovery uses fixed argv; manifestless/unreadable/zero-test targets cannot pass; both dogfood apps run real tests |
| DONE | Copy-anywhere init/upgrade | Fresh scaffold includes a real starter manifest/test and the complete governed harness; init/upgrade preflight every planned existing/backup leaf before the first mutation, while portable case/Windows aliases, inode aliases, source/target/state/backup/prune symlink and special-file attacks fail closed |
| DONE | Rust Agent Runtime offline slice (ADR 0006) | Deterministic provider/tool loop, bounded output/turn/tool calls, cancellation, strict JSONL event ordering, workspace capability confinement and exactly-one terminal failure contracts |
| DONE | Local Conversation Hub (ADR 0007) | Global/Project/Group CLI, SQLite Conversation/Prompt/Project/Group persistence, atomic/idempotent mutations, complete first-open retry, private DB/WAL/SHM and workspace-unavailable failure contract |
| DONE | Strict Hub schema ownership validation (ADR 0012) | Every canonical v0–v27 schema and the historical endpoint-only v25 migration source are validated before migration DDL (v0 must be empty), and the final v27 migration step validates the exact 39-table/38-explicit-index main catalog plus column/FK and all 100 implicit-index signatures. Per-version exact DDL expectations are regenerated from immutable migration batches; independent structural contracts are hash-pinned, and exact DDL validators lock CHECK-only changes that structural signatures intentionally omit. Old-object drift and rogue table/view/trigger/virtual shadows fail as corruption without repair; semantic backfill/final-validation failure rolls the migration transaction back |
| DONE | Local Group context dossier (ADR 0007 follow-on) | `group context` resolves Group discussion and member-Project Prompt history in one SQLite snapshot, preserves causal Run-answer placement, emits deterministic provenance/hashes/omission statistics under member/conversation/Prompt/content bounds, hides content by default, and excludes Global/other-Group/nonmember records, canonical paths, files, tools and network |
| DONE | Durable prepared Group Run snapshot (ADR 0009) | SQLite v3 atomically freezes one exact canonical Group dossier into an independent prepared Group Run; same-key retries replay original bytes without querying newer history, divergent inputs conflict, show verifies both digest layers and structural bindings, list reads metadata only, default output is redacted, and the management path creates no provider/tool/workspace or Project Run/event/assistant side effect |
| DONE | Local Group execution integrity receipt (ADR 0010) | SQLite v4 binds one independently versioned execution to a fully verified prepared Group Run and persists a compact three-event receipt journal; key-first creation pins an incomplete intent, exact per-event transactions advance its durable prefix, and same-key local recovery appends only a deterministic missing suffix before returning terminal success. Newer Prompts cannot alter the receipt, output excludes event/context bodies, paths, and keys, and the synchronous path constructs no model/provider/runtime/workspace/tool/network or Project Run/assistant side effect |
| DONE | Two-phase Group model analysis (ADR 0011) | SQLite v5 binds one verified prepared Group Run to one exact canonical zero-tool Responses request. Local prepare atomically records the request and first journal event; send requires fresh explicit consent plus credential/destination preflight, releases exact bytes through one exclusive claim, never retries an uncertain external effect, and atomically persists only a validated provider terminal. Default views omit request/config/event/result bodies, while explicit result output is terminal-safe; the path creates no workspace, Conversation, task, memory, Project Run, assistant Prompt, or tool side effect |
| DONE | Durable local Group analysis panel (ADR 0013) | SQLite v6 atomically freezes 2–8 unique terminal-completed analyses from one exact Group Run into a canonical ordered manifest and child bindings. Same-key replay preserves identity/order/bytes; changed, `length`, cross-source, missing or corrupt inputs fail closed. Default output redacts result text, show fully revalidates sources and copied artifacts, and the local-only path honestly claims assembly rather than discussion, synthesis or consensus |
| DONE | Two-phase Group panel synthesis (ADR 0015) | SQLite v7 binds one fully revalidated immutable panel to an exact canonical zero-tool request whose only user message is the ordered panel manifest. Prepare stays local and adds no separate dossier/excerpt fields (copied answers may themselves reproduce source content); send requires fresh consent, exact target/request preflight and a single-winner authority, never retries post-claim uncertainty, and persists only a metered zero-tool terminal plus true EOF. Default output hides copied results/request/result, fixed targets prohibit writeback, and the artifact is labeled single-model synthesis rather than discussion, consensus or factual verification |
| DONE | Durable local Group Agent Graph (ADR 0017) | SQLite v8 atomically freezes one exact Group Run, manager/profile labels, frozen member-project task assignments, canonical dependency edges and deterministic authored-order waves. Same-key replay compares the semantic manifest; cycle, endpoint, member-role, source, byte or digest drift fails closed. Default output hides the spec and all execution flags remain false because this slice prepares an interchange graph rather than running a manager or node Agent |
| DONE | Core-owned passive Group Agent Graph Run plan (ADR 0018) | `forge graph-plan` shares the Go orchestrator's single authored-order dependency implementation and emits a canonical, domain-digested topology plan. SQLite v9 admits only a plan matching one fully revalidated Graph, atomically stores an `awaiting_execution_contract` Run and one preparation event, and supports exact replay/show/metadata-only list. Execution-contract and dispatch-authority flags are fixed false; no manager/node/model/tool/workspace/result/writeback occurs |
| DONE | Core-owned first-node execution contract (ADR 0019) | Rust exports an exact private v1 control snapshot; Go alone selects the first node and freezes canonical Prompt/provider/budget/zero-capability/no-retry inputs; SQLite v10 admits one exact contract by seq/head CAS, appends event two, and transitions only to `awaiting_core_dispatch`. Same-key replay preserves identity/time/event/bytes, default output redacts private contract data, and dispatch authority remains false with no credential/model/provider/Agent/workspace/tool/result/writeback effect |
| DONE | Core-owned Node Dispatch Request preparation (ADR 0021) | Rust reconstructs the admitted first-node logical request and uses only the pure Responses codec; SQLite v11 stores one exact content-addressed body and seq-3 receipt by seq-2/head CAS, then stops at `awaiting_dispatch_authorization`. Replay/concurrency preserve the original identity/body/event/time, default output redacts private request inputs, and no consent, credential, provider, transport, network, workspace, tool, result, writeback, node advance, or dispatch authority is introduced |
| DONE | Effect-free Node Dispatch release authorization (ADR 0022) | Rust exports one exact private v3 release-control snapshot; Go independently reconstructs and validates the base control, source/plan/manifest, three-event journal, contract, request/body, lane, destination, budgets and no-retry policy before emitting a content-addressed authorization; Rust verifies that artifact against freshly reloaded current state. Schema remains v11 and the Run remains v3: authorization is not persisted and no consent, credential, provider, network, lane claim, result, writeback, graph advance, or dispatch-authority release occurs |
| DONE | Effect-free registered destination and pricing readiness (ADR 0023) | Go emits one canonical operator-asserted pricing snapshot for the fixed Responses destination; Rust independently verifies destination, model, token ceilings, checked component-rounded worst cost and the exact authorization budget. Public readiness reloads the durable v3 aggregate without reading credentials, constructing a provider, accessing the network, claiming a lane or mutating the Hub |
| DONE | One-shot single-node Dispatch terminal lifecycle (ADR 0024) | Fresh consent and an explicit credential may release exactly one persisted request only after a Hub-global Project-lane claim atomically advances the Run to v4/seq 4 `dispatch_unknown`. A bounded no-retry collector records a result or uncertainty artifact, pure Go Core emits the terminal receipt, and one v5/seq 5 transaction persists all evidence and releases the exact lane. Reentry after a stranded claim never resends; multi-node execution remains fenced in ADR 0024 itself. Later ADR 0034 and the 2026-08-08 resolution addendum separately close scheduled-family pid-sidecar and legacy-v4 no-send operator adjudication |
| DONE | Passive schedule-bound initial-node contract candidate (ADR 0026) | Go independently rebuilds the multi-node schedule from pristine control, matches only the caller-claimed digest without reading Hub state, selects ordinal zero, and emits a canonical v2 candidate with empty predecessor/receipt evidence. Rust/SQLite v14 then require that digest to identify the matching stored schedule and persist one immutable sidecar while leaving the Run/journal unchanged; immediate-transaction mutual exclusion permits either legacy v1 or candidate v2 but never both. Default views redact private inputs, and every false flag is scoped to the non-dispatchable candidate artifact itself, which contains no provider-request bytes, authority, progress, receipt, or successor advancement |
| DONE | Passive scheduled-node provider-request sidecar (ADR 0027) | Rust revalidates the exact ordinal-zero candidate and its full source graph, then uses the deterministic side-effect-free Responses codec to encode and validate one exact request body. SQLite v15 persists it in a distinct immutable sidecar while the Run remains v1/seq 1 and the main journal stays unchanged. Default views redact the body and private routing fields; no consent, credential, provider, network, workspace, lifecycle, dispatch, lane, progress, receipt, or successor authority exists, and the legacy dispatch family cannot discover or consume it |
| DONE | Effect-free scheduled-node dispatch authorization (ADR 0028) | Rust exports private canonical control only after exact current-v15 source and Responses-byte validation; Go independently rebuilds the schedule, candidate, logical request, provider request, lane, budgets and failure policy before emitting one content-addressed decision; Rust reloads the Hub and verifies the artifact against fresh state. Shared exact-byte golden, adversarial domain/application tests and a real Rust→Go→Rust CLI suite prove file/stdin parity, redaction, fail-closed malformed/stale/corrupt input and zero durable/network/workspace/credential effect. The decision authorizes only a future exact lifecycle admission plus execution/dispatch release: current admitted/released facts stay false, schema/Run/journal remain unchanged, and no consent, claim, send, progress, receipt or successor action exists |
| DONE | Effect-free scheduled-node dispatch readiness (ADR 0029) | Rust freshly revalidates current scheduled authorization and the existing exact Go canonical operator-asserted pricing snapshot, independently computes checked component-rounded maximum cost, resolves only an official registered destination quote and rejects any quote or metadata disagreement. Schema v15 and Run v1/seq 1 stay unchanged, readiness is not persisted, and output explicitly separates future-only authorization decisions from false current effects, including no provider request send. Domain/Application/registry/CLI adversarial suites and real Go→Rust E2E prove bounded exact decoding, file/stdin parity, drift rejection, redaction, byte-identical state and zero loopback connection; two fresh reviews and independent Codex black-box QA accepted the result |
| DONE | Independent scheduled-node effectful dispatch lifecycle (ADR 0030) | SQLite v16 adds a sidecar that atomically claims the scheduled ordinal-zero request and global Project lane only after fresh authorization/pricing/credential/provider preflight. A non-clone exact body authority is consumed once, bounded provider evidence is reduced to result/uncertainty, and a pinned Go Core validates terminal control and emits one scheduled receipt; evidence plus receipt terminalize the sidecar and release the lane in one transaction. Core/commit failure stores artifact-only quarantine and forbids resend. The scheduled Run remains v1/seq 1, legacy dispatch discovery is fenced, default CLI output is metadata-only, and protocol v1 has no retry/resume/lease/health/successor or live-network test path |
| DONE | Durable local Project Run journal (ADR 0008) | Transactional SQLite v1→v2 migration, execution-bound Run creation, O(1) incremental semantic cursor append plus same-snapshot full-prefix reconstruction, persistence-first delivery, tool-start-before-effect, terminal/incomplete/pending-tool inspection, atomic Run-authorized assistant writeback, and credential-free terminal replay repair all have positive/adversarial/cross-process tests |
| DONE | Bounded Conversation history bridge | A Project Run loads only lowercase user/assistant records causally before its bound user Prompt under a 16-message/512 KiB budget. Run answers stay anchored after their source user even when repaired later; the global budget keeps complete newer anchor groups and reserves the cutoff group's source plus newest fitting answers, contradictory associations fail closed, and the current Prompt enters exactly once |
| DONE | Opt-in OpenAI Responses provider boundary | `--live` requires an explicit idempotency key and `OPENAI_API_KEY`, exposes zero tools by default, and grants only repeatable exact `--allow-read` targets; the adapter fixes the HTTPS API origin, disables redirects/implicit retries, uses `store:false` with complete validated output-item replay, phase-aware final projection, streamed-to-terminal message/function identity and status checks, bounded transport/SSE/model output, and credential redaction. Only `max_output_tokens` maps incomplete to a normal limit; content filtering, contradictory/unknown terminal semantics, and all incomplete tool calls fail closed. Two-request contract tests use loopback only; no paid external request was run |
| DONE | Offline deterministic dependency scan | Committed OSV snapshot drives a real SCA PASS/FAIL without network during acceptance; missing DB remains visible rather than fabricated green |
| DONE | Local Docker and Firecracker sandbox runners | Sprint 68–71 wired both runtimes into `forge run/evolve --sandbox`; deterministic component tests cover stdin, output/timeout/memory bounds and fail-closed configuration, while host verification booted a real Firecracker microVM and a real Docker container. The current runner contract is still argv/stdin/output only and does not claim repository snapshot/mount or artifact sync-back |
| DONE | Cross-vendor LiteLLM host validation | 2026-08-05 host verification routed through LiteLLM to DeepSeek and local Ollama; the Ollama backend completed inference and the DeepSeek route reached the intended provider but hit its upstream monthly limit. This proves the local gateway/routing prerequisite, not a shipped production multi-provider registry |
| ADOPTED-PLANNED | Capability-centric AI Engineering OS governance layer (ADR 0037) | `docs/design/ai-engineering-os/` fixes the 00–16 lifecycle SOP, conditional engineering/refactoring rules, Evidence/Claim/Graph/Impact/Cost/Risk/ADR/Debt/Health/Grant/Review contracts and a `planning_only` capability catalog. ADR 0040 provides the cross-cutting governance entry; ADR 0045–0066 now include bounded pure/local/shadow/contract-only, bootstrap authority, strict review/digest-binding, and partial GraphSnapshot foundations recorded here. No semantic truth/authoritative current-knowledge ledger, authority-bearing lifecycle promotion/conflict arbitration, Knowledge apply/receipt, capability invocation registry, authoritative full graph, complete Grant/PDP/Approval/Transition lifecycle, provider Context Router runtime, full ReviewCase/normalized finding/identity/SoD or complete role/workflow expansion is implemented. |
| DONE | Evolve repository locator → EvidenceRecord shadow adapter (ADR 0050) | Exact caller-declared repo locator/content/scan-context/producer/source observation plus explicit Governance binding maps deterministically to existing `repo_locator` EvidenceRecord across Python/Go/Rust and universal exact reprojection. It does not read current repository files/reports, resolve symlinks, verify digest preimages, confirm scan judgments/completion, authenticate producers, persist records or grant truth/authority/effect; Schema/golden/Skill/detector and init/upgrade propagation are pinned |
| DONE | Local Governance semantic view v1 (ADR 0054) | Rust domain/application/SQLite/CLI materialize and independently revalidate exact current aggregate-tail projections; caller `as_of_unix_ms` evaluates that tail and never selects history. The view enforces the authority-free Claim lifecycle subset, lists deterministic conflict candidates without a winner, and schedules Assumption/Hypothesis validation jobs without execution or verdict. Exact-v27 live reads use one read-only Deferred snapshot with explicit per-view and scan-wide record/byte/work budgets. SQLite v27 atomically revalidates batches, complete histories and reference relations before backfilling exact v26 journal records, and rolls back on corruption; schema/golden/registry/Skill/scaffold assets pin `semantic_projection_only_no_truth_or_authority` |
| DONE | Shadow ContextPackage v1 pure contract (ADR 0055) | Strict exact request plus Python/Go/Rust implementations deterministically reserve required context, rank optional context, separate typed trust lanes, apply declared UTF-8 byte redactions/truncation, count a tokenizer-pinned canonical projection, emit omission/accounting/freshness receipts, and bind request/snippet/projection/context/cache digests to one exact golden. Full reassembly is required for package/cache validation. The builder performs no repository/network/process/provider/database I/O and grants no truth, instruction, permission, approval, completion, persistence or effect authority; live Context Router/prompt compiler/source authentication and Grant/PDP integration remain planned. |
| DONE | CapabilityGrant v1 contract-only wire (ADR 0056) | Strict exact envelope and authority-neutral declared assessment freeze one-effect vocabulary, typed allow/deny scope, budgets, bindings, caller-time validity and SoD declarations across Python/Go/Rust. `CapabilityGrant` remains in `shipped_contract_only_kinds` alongside the ADR 0059 contract-only ApprovalRecord; its detector is shadow/non-load-bearing, and scaffold installs only the universal checker. No issuer/proof/principal/policy/Approval authentication, PDP decision, revocation/usage evaluation, pre/postflight, audit receipt, persistence, execution, authorization, permission or effect authority is implemented. |
| DONE | Authenticated bootstrap repo-read Grant issuance profile (ADR 0057) | The separate, operator-deployed non-Agent `forge-kernel` authenticates an externally pinned trust root, signed policy and signed GrantRequest, then can issue only a signed `CapabilityGrant` plus durable signed `GrantIssuanceReceipt` for `bootstrap_planning`, `repository-reader/v1`, `repo.read`, exact paths, bounded TTL/budgets and `local|development|test`. Runtime support is Unix-only and non-Unix fails before authority inputs or key material are read. Resolved authority/repository endpoints must be absent from one another's ancestor chain by filesystem identity; the caller repository source, initial resolution and opened directory identity stay bound for the session. Authority/state directories are exact `0700`, while closed relative leaves are effective-UID-owned, single-link regular files with exact `0600`, no special mode bits and no symlink ancestry. These local checks are not OS-principal or HSM isolation, and same-UID tests are not a production trust proof. Ledger high-water is relative only to the current signed snapshot; v1 has no TPM, remote witness or external monotonic anchor and cannot resist an administrator replaying an older signed snapshot after replacing local authority state. The structural detector remains shadow/non-load-bearing; scaffold/upgrade copies no kernel, key, trust root or state and reports `not_executed` without a compatible external runtime. Plan finalization, all other effects/capabilities/environments, Approval, revocation, usage/reservation, pre/postflight, PEP/effect execution, ContextPackage/provider/Transition/Knowledge integration, key provisioning/rotation, remote/HA/multitenancy and the complete Governance Kernel/PDP remain planned. `forge accept` is completion authority, not an issuer. |
| DONE | Authenticated bootstrap repo-read execution profile (ADR 0058) | An independently pinned execution root and exact signed execution policy/invocation constrain the one ADR 0057 Grant to Linux amd64/arm64 `openat2` reads of 1..16 sorted manifest-bound regular files with exact raw byte length/SHA-256. Durable single-use state is reservation → intent → completed/failed-consumed/quarantined; active tails never resume or reread. The ledger stores signed receipts and content-free metadata, never raw content; exact replay occurs before manifest/repository/clock/receipt-seed access and is receipt/metadata-only. Timeout is cooperative, administrator replacement of the complete signed state can roll back the snapshot, and best-effort mutable-buffer clearing does not prove secure erasure of strings/GC/kernel/downstream copies, process isolation or HSM custody. Root, receipt key and usage namespace are indivisible; v1 has no rotation, epoch migration or state clear/rebase, and fresh state inherits no spent history. Production rejects the public fixture root/keys. Scaffold copies only ADR/schema/fixture/Python structural/governance checks, never binary/root/key/state, and reports `not_executed` without a compatible runtime. Approval/revocation, writes/network/process/secrets/targets, staging/production, Context reassembly, generalized PDP and remote/HA/multitenancy remain unavailable. `forge accept` is completion authority, not execution authority. |
| DONE | ApprovalRecord v1 contract-only wire (ADR 0059) | Strict bounded record, detached-proof content identity, exact declared target/request/assessment and ADR 0056 ApprovalRef projection are reproduced across Python/Go/Rust and formally accepted. Registry v16 keeps it in `shipped_contract_only_kinds`; the detector remains shadow/non-load-bearing, and the trusted schema route, policy-authority Skill and universal init/upgrade projection are wired. Local `.approved` markers, caller flags, actor hints and workflow/session state are never imported. Approver/authority/proof/SoD authentication, condition/RiskAcceptance/revocation validation, effective approval, PDP/authorization, permission, persistence, transition and effects remain unavailable. |
| DONE | TransitionReceipt v1 contract-only wire (ADR 0060) | Strict state vocabulary, receipt/target/request/assessment identity, explicit predecessor/recovery/applicability declarations and ADR 0056/0059 reference comparisons are reproduced across Python/Go/Rust and formally accepted. Registry v17 classifies `TransitionReceipt` as contract-only and leaves only `KnowledgeUpdateProposal` planned; detector/route/Skill/scaffold remain shadow and authority-neutral. No controller/current-state/precondition/waiver/Grant/Approval authentication, ledger/CAS/replay, persistence, transition, completion or effect exists; Accepted/DONE does not create Transition or runtime authority. |
| DONE | KnowledgeUpdateProposal v1 contract-only wire (ADR 0061) | Strict proposal/target/request/assessment identity, exact ADR 0045 reachable record closure, sorted create/supersede declarations and declared CapabilityGrant/ContextPackage/artifact compatibility are reproduced across Python/Go/Rust and formally accepted. Registry v18 classifies the wire as contract-only with no planned kinds; detector/route/evidence Skill/scaffold remain shadow and non-load-bearing. No proposer/Grant/Context authentication, Evidence truth, authoritative current head, conflict/freshness/policy decision, truth/adoption/authorization/permission, persistence/apply/receipt/execution/effect exists; Accepted/DONE does not create Knowledge authority. |
| DONE | Planning Capability Ownership Projection v1 (ADR 0069) | Independent Python/Go strict pure projectors consume only caller-supplied exact planning catalog/mapping bytes and reproduce one physical golden with 17 nodes, 145 occurrences, 140 unique fine capabilities, 38 declared packages and 140 exactly-one-primary-owner bindings. Product CLI is exactly `forge capability-ownership project --catalog FILE|- --mapping FILE|-`; governance registry v24, shadow detector, source/Schema/golden pins and source-only init/upgrade scaffold are wired. Derived `.agent/skills/*.md` locators stay unresolved (`not_performed`/`not_evaluated`); the slice generates no owner Skill/host adapter, mutates no Registry, authenticates no owner, and supplies no Grant/PDP/CapabilityInvocation/routing/persistence/transition/effect authority. |
| DONE | `project-snapshot` narrow package slice (ADR 0070) | Linux-only Go live producer/CLI, independent strict Python checker and exact golden, a closed portable `skills/project-snapshot/` package, `.agent` adapter, registry v25 shadow wiring and fresh/legacy scaffold close only the nested project-snapshot item. The two-endpoint observation binds allowed regular worktree bytes, tracked-absent facts, hashed pre-read path exclusions and honest coverage; it remains non-atomic/non-current/incomplete, path policy is not content secret scanning, Git/HEAD are unauthenticated, and copied source is neither installed runtime nor permission/Graph/Registry/Grant/PDP/CapabilityInvocation/persistence/routing/effect authority. |
| DONE | `context-engineering` narrow package slice (ADR 0071) | A closed 16-file portable package, zero-argument exact-stdin assembler, strict physical manifest checker, registry v26 shadow wiring and fresh/legacy scaffold package the unchanged ADR 0055 ContextPackage v1 pure projection. The slice discovers no source, calls no provider/model, compiles no live prompt, copies no Catalyst Go/Rust runtime, installs no host Skill, authenticates no publisher, and provides no check-to-use atomicity, Grant/PDP/Approval, truth/instruction/completion/persistence/routing/effect authority; only the nested item is closed while the 38-package parent and remaining 36 items stay open. |
| DONE | `evidence-claim-management` narrow package slice (ADR 0072) | A closed 18-file portable package, zero-argument explicit-EOF exact-stdin validator, strict physical manifest checker, registry v27 shadow wiring and source-only fresh/legacy scaffold package the unchanged ADR 0045 structural validator. The slice accepts only already-authored records; it observes, authors, repairs, sorts, seals, returns and persists nothing, reads no ambient source/journal/semantic view/proposal, installs no host Skill, and provides no truth/instruction/Grant/PDP/Approval/completion/routing/transition/execution/effect authority; only the nested item is closed while the 38-package parent and remaining 35 items stay open. |
| DONE | `policy-authority` narrow package slice (ADR 0073) | A closed 30-file portable package, two independent zero-argument explicit-EOF exact-stdin declared-assessment adapters, strict physical manifest checker, registry v28 shadow wiring and source-only fresh/legacy scaffold package the unchanged ADR 0056/0059 pure evaluators. The slice adds no combined envelope, Grant issuance/activation, effective Approval, live policy/identity/revocation/usage, Kernel/PDP/PEP, host installation, authorization/permission/completion/persistence/routing/transition/execution/effect authority; only the nested item is closed, and the 38-package parent plus remaining 34 items stay open. |
| DONE | `adr-governance` narrow package slice (ADR 0074) | A closed 25-file portable package, exactly-one-basename-argument explicit-EOF exact-stdin validator, strict physical manifest checker, registry v29 shadow wiring and source-only fresh/legacy scaffold package the unchanged ADR 0067 Proposed-only evaluator. The caller basename is an independent lexical label, not physical file/repository proof. The slice adds no domain envelope, repository scan, authoring/repair/reseal, acceptance/compliance/lifecycle, DECISIONS query, legacy import, Go `writes_adr` runtime, host installation, persistence/execution/effect authority; only the nested item is closed, and the 38-package parent plus remaining 33 items stay open. |
| ADOPTED-PARTIAL | AADM Decision Kernel, Capability ABI and Meta Reflection (ADR 0038) | ADR 0090/0091 supply the completed narrow structural reference-family repository slice for CognitiveAtom v2, DecisionTransaction v1 and one-way InteractionEvent/CapabilityInvocation/ArtifactReceipt/ExecutionReceipt references, with exact Python/Go/Rust parity and all 22 authority/effect attestations false. Both ADRs remain Proposed. ADR 0068 delivers the Authority-neutral Capability Registry v1 only as a staged singleton, and ADR 0069 only an authority-free planning ownership projection. The broader target remains partial: DecisionCapsule, AuthorizedTransactionSpec, authenticated Grant/PDP integration, authority-bearing package implementations and physical adapters, runtime routing, CAS/event append/persistence, solver/rolling controller, R0–R2 Reflection, authoritative lifecycle and adaptive rule updater are unimplemented. |
| DONE | Kernel structural reference-family ABI repository slice (ADR 0090/0091) | Registry v38 preserves the complete scope mapping and digest, adds one checker-only pinned-golden shadow, and source-distributes exactly 19 Python core/governance files. Catalyst exact13 Go, flat exact9 Rust and shared `lib.rs` registration remain repository-only. Declared source/authority/hardness/bindings are not authenticated, instructions remain disabled, and no Skill, route, kind, evaluator, producer, runtime profile, PDP or controller is added. Both ADRs remain Proposed; formal `forge accept` is ACCEPTED (9/0/2, Python 1323, Node 460, Go 2466, coverage 83.45830729709088%), so the narrow implementation item is complete. |
| PENDING | Decision Capsule structural replay repository Candidate (ADR 0092/0093) | Registry v39 preserves the complete scope mapping and digest and adds one checker-only pinned-golden shadow. Exact19 distribution is Python-only; Catalyst exact15 Go, exact14 Rust and shared registration remain repository-only. The four-object DAG only validates/reseals/projects/compares caller-supplied structure; dedicated later Reflection attachment refs remain unresolved and outer-only, while upstream ArtifactRefs remain opaque and uninterpreted. All 32 attestations, both replay controls and all seven completion claims are false; there is no Skill, route, runtime, model/rule/history/Reflection consumer, persistence, PDP or controller. Both ADRs always remain Proposed/null, the narrow checkbox stays open pending independent review and formal `forge accept`, and ADR-0038, full DecisionCapsule and AuthorizedTransactionSpec remain incomplete. |
| ADOPTED-PLANNED | Default-off Device-Aware Execution Fabric (ADR 0039) | The target design separates logical Agent/Task/Attempt/ExecutionTarget; stages Local ABI → Inventory/Observe → structured SSH → mTLS Runner/Lease/Fencing → Placement/Controller → safe migration → Federation, with CAS artifacts, zero-trust data/egress, LOST/quarantine and external production operator invariants. Existing local Docker/Firecracker runners are not a Device Registry, remote Runner, scheduler or migration fabric; no remote access is authorized or implemented by this plan |
| DEFERRED-BY-DESIGN | Independent `agent-os`, remote Hub/account/sync and Web UI | ADR 0003 still needs a remote location and migration approval; remote identity/ACL/sync remain later phases, and Web UI remains part of the v3 target architecture |
| NON-GOAL | Real remote deploy/rollback inside ForgeOS | ADR 0005 explicitly limits ForgeOS to generation, validation and human approval of a declarative package; separately governed external CI/operators own remote execution |

---

## DONE

| Requirement | Source | Evidence |
|---|---|---|
| Local Conversation Hub selects Global with no path and a canonical Project with a bare path/`-C` | ADR 0007; CURRENT_SPRINT.md Sprint 34 | `interfaces/args.rs` + `hub_command.rs`; process-level CLI tests cover both selectors and JSON scope |
| Project/Conversation/Prompt/Group state survives CLI processes in a versioned local SQLite store | ADR 0007 | `infrastructure/sqlite_hub`; persistence/reopen, schema, idempotency, concurrency, permission and CLI process tests |
| Local Groups link frontend/backend/SSO Projects by descriptive role and own discussion Conversations | ADR 0007 | `group_projects` store contract + CLI integration test; roles are explicitly non-ACL labels |
| Hub mutations are retry-safe and Group linking is atomic | ADR 0007 | Caller-reusable `--idempotency-key`; Project registration + Group link share one immediate transaction; process tests cover replay/conflict and rollback on a missing Group |
| Effectful successor dispatch through the ordinal-N chain | ADR 0032; CURRENT_SPRINT.md Sprint 61–62 | Domain admission source validation branches by contract scope (initial path byte-identical); admission records and provider-request/dispatch-release contracts accept ordinals 1..=31; the effectful execute path (ADR-0030) is untouched — fresh consent, atomic claim + lane, bounded collector, pinned Core receipt, terminalize + lane release, no-resend quarantine; same-project nodes serialize via the exclusive_until_terminal lane policy |
| Predecessor content disclosure with independent consent | ADR 0033; CURRENT_SPRINT.md Sprint 64 | The request-v2 user Prompt gains an optional `predecessor_output` field (omitempty; existing goldens byte-identical); Go Core embeds bounded exact UTF-8 predecessor result text and sets `predecessor_content_included`; Rust enforces flag/field co-presence, admission verifies the content byte-for-byte against the prompt and the durable result-class artifact (uncertainty not disclosable); effectful dispatch execute demands a separate `--confirm-predecessor-content` never inferred from `--confirm-off-machine` |
| G3 multi-dimensional auto-scorer drives real execution | PROJECT.md G3; CURRENT_SPRINT.md Sprint 63 | `routing.FromChangedPaths` derives six dims (complexity/context/dependency/business-impact/risk/security) from changed paths; run/evolve tier resolvers apply raise-only score escalation before the budget down-tier; `forge route --diff-files/--from-git` auto-fills unset dims (explicit flags authoritative); no-diff runs stay byte-for-byte backward compatible |
| Passive successor contract candidate consuming verified predecessor receipts | ADR 0031; ADR 0035–0036; CURRENT_SPRINT.md Sprint 60, 74–86 | Rust exports exact canonical receipts from terminalized scheduled lifecycles. Go Core offline-validates caller evidence in arbitrary order as canonical, identity-bound `completed`/result-shaped receipts, canonicalizes the candidate to the schedule's complete direct-predecessor order, then selects a topology-ready successor or an explicitly requested ready target. Rust/SQLite v17–v24 admits the immutable per-node sidecar only after byte-for-byte durable-lifecycle verification. ADR 0033 optionally binds ≤1 MiB exact predecessor result text to the canonical first direct receipt; `ready-nodes`/`wave-admit` expose the wave plan without fabricating progress. Candidate construction itself performs no dispatch, lane claim, credential access or successor execution |
| Hub v0–v27 schema ownership is fully revalidated | ADR 0012, subsequent Graph protocol ADRs through 0036, journal ADR 0046 and semantic-view ADR 0054 | `schema_contract/full_contract.rs` generates every canonical prefix contract from immutable DDL and checks exact definitions, full catalog, `table_xinfo`, FK and named/implicit index signatures before migration and at the v27 commit; an exact validator also admits only the historical endpoint-only v25 migration source. The additive v27 projection migration revalidates exact journal batches and atomically backfills semantic heads/Claim fields/validation jobs, rolling all changes back to exact v26 on any final failure. Adversarial tests cover legacy generations, both exact v25 lineages, malformed hybrids, empty-v0 enforcement, rogue catalog objects, distinct release goldens, error classification and whole-chain rollback |
| Completed Group analyses can be frozen into one durable ordered local panel | ADR 0013; CURRENT_SPRINT.md Sprint 42 | Domain/application/store validation rejects duplicates, `length`, nonterminal and cross-source artifacts; SQLite immediate creation and ordered member rows have replay/concurrency/corruption tests; CLI prepare/show/list defaults to redacted content and explicitly labels the result assembly-only |
| One immutable Group panel can produce a separately consented synthesis | ADR 0015 | Domain/application/store validation rebuilds the exact panel-manifest request before prepare and claim; the three-event SQLite lifecycle grants only one consuming dispatch authority and preserves uncertainty without resend; CLI prepare/send/show/list defaults to redacted, no-writeback, single-model output |
| One frozen Group Run can define a durable manager/Agent dependency graph without executing it | ADR 0017 | Domain/application/store validation binds exact source and member roles, canonicalizes edges, derives stable Kahn waves, and checks canonical bytes/digest on prepare/show; CLI process tests cover redaction, exact replay, list metadata, cycles, member mismatch, explicit spec-file disclosure, and zero execution/provider/workspace-scan/tool/writeback claims |
| One multi-node Graph can freeze a durable Core-owned serial policy without executing it | ADR 0025; CURRENT_SPRINT.md Sprint 54 | Shared Go/Rust goldens bind canonical schedule bytes, content ID, authored-order ordinals, Project lanes, direct-predecessor receipt slots, initial frontier and fail-fast/no-dataflow policy. Rust domain/application/SQLite v13 revalidate exact base control, preserve the main Run/journal, and enforce corruption-first replay/concurrency; a real Go→Rust CLI test proves default node/lane redaction, explicit reveal, exact replay and zero credential/provider/network/workspace/tool/result/successor effects |
| One multi-node Graph can freeze a passive schedule-bound initial-node candidate without dispatch | ADR 0026; CURRENT_SPRINT.md Sprint 55 | Shared Go/Rust fixture tests bind canonical candidate/request bytes and digests, Core rebuilds rather than trusts the schedule and admits no caller node/receipt, and Rust domain/application/SQLite v14 enforce source-bound validation plus v1/v2 mutual exclusion. Real Go→Rust CLI tests prove a logical before/after match for every non-candidate Hub table, removed member workspaces, a poisoned credential sentinel, a zero-connection endpoint, default redaction, explicit reveal, exact replay, and malformed/pre-Hub rejection; transactional tests separately prove stale-head and corruption-first behavior |
| One admitted scheduled candidate can freeze its exact provider request without dispatch | ADR 0027; CURRENT_SPRINT.md Sprint 56 | Domain/application tests bind deterministic codec output and prove the codec/store call trace; SQLite v15 tests cover full source reconstruction, migration, replay, concurrency, identity substitution, corruption precedence, drift and rollback. A real Go schedule/candidate→Rust request E2E compares stored BLOB bytes to explicit CLI reveal after member workspaces are removed, with poisoned credentials and a zero-connection endpoint; legacy prepare/show/list/release/execute remain isolated, and a sentinel executable proves rejected legacy execute does not start Core or write state |
| One Core-owned first-node contract can be durably admitted without dispatch | ADR 0019; CURRENT_SPRINT.md Sprint 48 | Shared Go/Rust golden tests lock control/request/contract bytes and digests; application/store tests cover exact v1 export, v2 replay reconstruction, CAS, corruption precedence, concurrency and rollback; a real Go-process-to-Rust-CLI test covers redaction, explicit disclosure, v2 transition and unchanged durable ID/time/event/contract bytes on replay |
| One admitted first-node request can be frozen byte-for-byte without releasing dispatch | ADR 0021; CURRENT_SPRINT.md Sprint 50 | Domain/application/store tests bind the exact Responses body, content identity, destination, pricing, contract and seq-2 head; SQLite tests cover migration, replay, corruption precedence, concurrency, CAS and late-reread rollback; a real Go-to-Rust CLI test removes member workspaces, uses an unreadable credential sentinel and zero-connection loopback endpoint, verifies default redaction/explicit reveal, and observes only the v3 passive transition |
| One prepared first-node request can receive an independently verified but still passive release decision | ADR 0022; CURRENT_SPRINT.md Sprint 51 | Shared Go/Rust exact-byte golden tests bind the release-control and authorization digest domains; adversarial tests reject outer-snapshot-resigned source/head/request/body/lane/destination/pricing/budget/failure drift plus a fully self-consistent substituted base control; a real Rust→Go→Rust CLI round trip compares database bytes before/after, removes member workspaces, uses a no-connection endpoint sentinel, checks raw no-LF artifacts and redacted honesty flags, and leaves the v3 Run without authority |
| The passive first-node release decision can verify one registered destination and immutable pricing bound without dispatch | ADR 0023; CURRENT_SPRINT.md Sprint 52 | Go emits an exact no-LF `operator_asserted` pricing artifact; shared Go/Rust golden tests bind destination/pricing digests and the independently rounded integer cost, Rust domain and registered-factory adversarial tests reject metadata, pricing, budget, and explicit-credential drift, and a real Go-pricing/Go-authorization→Rust-readiness CLI path preserves every state byte while reporting consent/provider/network/lane/result/database/advance effects as false |
| One authorized single-node Graph can claim, dispatch once and terminalize without an unsafe resend | ADR 0024; CURRENT_SPRINT.md Sprint 53 | Shared Go/Rust goldens bind seq 4, claim, lane, result/uncertainty, terminal control, Core receipt and seq 5. Application tests use an injected deterministic provider for completed/length/uncertain outcomes, HTTP/transport/protocol classification, true EOF, consent/cancel fences and post-claim quarantine; SQLite tests cover competing claims, cross-Run lane ownership, v11→v12 migration and write-fault rollback; Infrastructure executes the real pinned Go Core bridge, while process-level CLI tests cover arguments/Core pin, exact-v11 multi-node zero-mutation rejection, consent/credential safety and redacted quarantine without a live provider |
| One scheduled ordinal-zero Graph request can claim, dispatch once and terminalize without legacy-journal mutation | ADR 0030; CURRENT_SPRINT.md Sprint 59 | Rust/Go scheduled terminal types bind the prepared-request identity separately from its exact body digest, enforce closed-world result/uncertainty evidence, canonical byte counts, cross-source control/artifact/receipt bindings and v16 corruption-first reads. SQLite uses immediate claim/terminal transactions, a unique active Project-lane index, replay-safe metadata output and artifact-only quarantine; the pinned Go Core handshake is subprocess-tested and legacy lifecycle commands reject scheduled sidecars. No provider/network/workspace/successor path is exercised by the test suite |
| Persistent lifecycle promotion can trigger the declared governance migration without transient-flag side effects | ADR 0016; modes.yml `auto_trigger` | `forge migrate --to-lifecycle production [--apply]` implements the known mode/lifecycle matrix; manual and lifecycle paths share a durable intent WAL, repository lock, exact-image CAS, operation receipts and deterministic recovery. Dry and run/evolve overrides are zero-write; terminal receipts are revalidated before execution and surfaced by status |
| Sensitive Prompt ingestion can avoid argv and add-response echo | ADR 0007 | `prompt add SESSION_ID -` reads bounded UTF-8 stdin; add returns a body-free receipt while explicit `prompt list` remains plaintext |
| Reviewer `REQUEST_CHANGES` triggers directed loop-back on agent phases, not just gate phases | ROADMAP.md:35-51 | `orchestrator.go:321 agentOutcome` + `cost.go:330 parseReviewerVerdict`, shares `loopBackTo` with gate path |
| Reviewer's specific feedback reaches the implementer via a non-polluting one-way edge | ROADMAP.md:50-51 | `prompt_context.go:187` feeds verdict forward without re-injecting into reviewer's own fresh context |
| `scorecards.json` auto-written after `forge run/evolve` | ROADMAP.md:58-70 | `cmd/forge/scorecard_wind.go:88 runScorecardUpdate`, fail-loud-and-continue |
| Cost/trace events carry the routed model name for scorecard attribution | ROADMAP.md:60-61,68-69 | `cost.go:468 Model: model` |
| `forge route`'s `HistoryTiebreak` actually influences real `--model` selection in `forge run/evolve` | ROADMAP.md:57-70 | `engine_build.go:288 logPhaseHistory` on the real `phaseTierResolver` path |
| arch-check sees JS/TS `export … from`, `export *`, dynamic `import()` | ROADMAP.md:75-79 | `harness/arch/scan.mjs:101-108 extractJsImports` |
| Function-length gate catches Python `async def` | ROADMAP.md:79-81 | `harness/arch/scan-functions.mjs:169` |
| secret-scan catches `.env` files structurally, not by extension | ROADMAP.md:81-83 | `harness/secret-scan.mjs:68,75-76` matches by basename |
| `forge accept`'s Node test globs are fail-closed | ROADMAP.md:83-84 | `harness/acceptance.mjs` `runCountedTest` requires `count>0`; recursive discovery/counting lives in `harness/acceptance-tests.mjs` |
| check.py validates workflow control-flow refs resolve to real targets | ROADMAP.md:85-86 | `harness/workflow_control_check.py` validates the control graph and `harness/check.py` registers it in `CHECKS` |
| forge-init's copy-list is guarded against silent drift | ROADMAP.md:94-95 | `harness/scaffold/forge-init.mjs` + `test_forge-init.mjs:108` manifest-integrity guard |
| Rate-limit/529/overload errors classified retryable, not terminal | ROADMAP.md:100-102 | `command_executor.go:96-97,207-210 classifyRunErr` → `KindOverloaded`; `backoff.go` |
| Checkpoint resumes at phase granularity, not just iteration granularity | ROADMAP.md:103-105,118-121 | `persist/checkpoint.go:59 PhaseIndex` + `orchestrator.go Engine.OnPhase` + `evolve.go:344 phaseCheckpointHook` (honest boundary: in-flight phase re-runs once on crash mid-phase) |
| Spawned agent subprocesses die as a process group | ROADMAP.md:106-107 | `command_executor_unix.go:49 Setpgid:true` + group SIGKILL |
| Prompt-injection memory lane has a hard cap | ROADMAP.md:108-109 | `prompt_memory.go:48 memoryCap = 32` + recency-floor/relevance mix |
| Run-level budget has a hard dollar cap surviving `--resume`, drives tier downgrade | ROADMAP.md:97 | `--run-budget-usd`/`--agent-max-budget-usd`; `route.go:355,358 budget_guard`; cost persisted across resume |
| Opt-in parallel execution of `depends_on`-independent phases, default serial unchanged | ROADMAP.md:127-142 | `orchestrator/parallel.go:67 RunParallel` + `waves.go` Kahn sort + `--parallel` flag; all 7 shipped workflows declare zero `depends_on` |
| Context (ROADMAP/ADR/AGENTS.md) not re-gathered/re-tokenized every phase within one run | ROADMAP.md:131-132,137 | `internal/prompt/cache.go:66 ContextCache` + `GatherCached` |
| Incremental test selection is advisory only, never replaces the full `forge accept` gate | ROADMAP.md:143-145 | `harness/select-tests.mjs` not imported by `acceptance.mjs`/`gates.go`/CI; fail-closed mapping |
| `.agent/` doc skeleton, `gate.mjs`, 2 ADRs, `.gitignore` | .agent/ROADMAP.md:6-9 | `node harness/gate.mjs` → `PASS (477 files, <=500 lines/file, root <=15)` |
| Function-length / circular-dependency machine-enforced | .agent/ROADMAP.md:10 | `.arch/rules.yaml` + `harness/arch/arch-check.mjs` (8 checks) |
| CC PostToolUse hook auto-runs `gate.mjs` on Edit/Write/MultiEdit | .agent/ROADMAP.md:11 | `.claude/settings.json` PostToolUse hook, exit 2 on FAIL |
| `forge check` asset validator; `forge accept` aggregation → ACCEPTED/REJECTED with honest N/A | .agent/ROADMAP.md:15,17 | `harness/check.py` registers 13 checks, including the Agent Engineering v1 aggregate; the full Python discovery suite covers governance/release helpers; `acceptance.mjs` composes gate+check+tests+app-tests; lint/coverage remain honestly N/A when their configured tools are absent, while SCA is a real PASS/FAIL gate |
| Dogfood app (`examples/url-shortener`) built end-to-end and gated by `forge accept` | .agent/ROADMAP.md:18 | `examples/url-shortener/{src,test,SPEC.md,README.md}`, 5 test files |
| `forge evolve` unattended loop: phases→gate→converge→loop with max-iter / no-progress tripwire | .agent/ROADMAP.md:19 | `orchestrator/loop.go:14-37,107-114 NoProgress` tripwire + `MaxIter`; convergence criterion-based, never round-count |
| Real `--executor=command --agent-cmd claude` end-to-end (task injection/acceptEdits/model/cwd/budget/trace-latency/cost-telemetry/reviewer-gate-signal) | .agent/ROADMAP.md:24 | `engine_build.go:122 --permission-mode`, `command_executor.go` cwd, `main.go` budget flags, routing wiring, `agentOutcome` |
| forge-core pure-Go, zero external dependencies | .agent/ROADMAP.md:22 | `go.mod` has no `require` block; `go build`/`go vet` clean |
| G1: requirement auto-discovery → PRD before code (mechanism) | PROJECT.md:11 | `discover.yml` P1-P3 emits `prd.md`; `researcher.md` agent card. *(Contract gap — see GAP: `requires_tools` degrade — is nested inside this row's Discover mechanism.)* |
| G2: architecture design staged by lifecycle, not day-1 gold-plating | PROJECT.md:12 | `mode.Policy`/`mode_gating.go` forces full review under `production` regardless of mode; `design.yml` Proposal-Generator → human approval gate |
| G3: risk≥critical forces Opus safety floor | ARCHITECTURE.md:49 | `internal/risk/risk.go:80 Classify` → `Critical`; `route.go:129-133`; `routing.opusFloorAgents`; auto-derived from changed paths |
| G3: budget guard downgrades tier under spend pressure | ARCHITECTURE.md:49 | `routing.BudgetAdjustTier` in `engine_build.go:251`, safety-floor agents exempt |
| G3: history-based tiebreak from Eval scorecard wired to real routing | ARCHITECTURE.md:49 | `HistoryTiebreak` — see `engine_build.go:288` row above |
| G4: Gap analysis drives roadmap items via agent, not hardcoded logic | PROJECT.md:14 | `evolve.yml` P1-P3: scan(explorer)→gap-analysis(architect)→roadmap-update(planner, `feeds_forward:true`) |
| G5: Scan→Gap→Roadmap→Implement→Harness→Review→Evaluate→(loop) closed loop | PROJECT.md:15; ARCHITECTURE.md:29-30 | `evolve.yml loop.loop_back_to: scan`; dedicated post-implementation `agent:harness` phase; `orchestrator.RunFrom`/`LoopEngine` with convergence + tripwire |
| Human Approval between Design→Build is judged by approval alone, distinctly reported | PROJECT.md:23; ARCHITECTURE.md:14 | `.forge/<stage>.approved` marker / `--approved` flag; `converge.IsHumanGate`/`reportHumanGate` |
| mode × lifecycle central knob drives Router tier / harness strictness / workflow depth | ARCHITECTURE.md:27-32 | `internal/mode/mode.go Effective(mode, lifecycle)` applies production as a monotonic quality floor without widening `evolve_authority`; orchestrator mode gating enforces stage skips, CTO build halt, and a name-independent proposal cutoff at the workflow's unique Agent-write `effect: mutate` boundary |
| Out-of-band harness gates are sole source of truth; host hooks are accelerators only | ARCHITECTURE.md:33-37 | CC PostToolUse hook additive/non-authoritative; `forge accept` is the real Stop gate CI relies on |
| 5 Engines landed: Orchestrator, Model-Router, Context-Engine, Memory-Engine, Evaluation-Engine | ARCHITECTURE.md:42-44 | `internal/{orchestrator,routing,prompt,memory,converge}` all exist, build, wired into `cmd/forge` |
| Agent-execution runtime — a real, working slice (not the full north-star microservice) | ARCHITECTURE.md:45 | `command_executor.go` spawns real `claude` processes with retry/backoff/timeout/process-group/budget/checkpoint |
| v0–v1: orchestrator = Claude Code native, no custom runtime; deliverables limited to declarative assets + thin glue | adr/0001:12-14 | `harness/gate.mjs` thin glue; `.agent/{agents,skills,workflows,policies}` predate forge-core |
| Supersession: forge-core built, zero external deps, CLI has `run/evolve/gate/check/accept`(+`migrate`/`route`) | adr/0001:5 | `go.mod` no `require`; `main.go:69-76` full dispatch table plus `detect/validate/status/scorecard/doctor/preflight/approve` |
| Target stack: `forge-core`=Go for orchestration/scheduling/routing/workflow | adr/0002:11 | ADR 0002 采纳时基线为 `cmd/forge` + 20 `internal/*` directories；当前 `go list ./...` 实测 61 包（59 internal + 2 cmd，排除 test-only `internal/adr` 后产品包 60），`go build`/`go test` 通过 |
| Insert REVIEW stage: Discover→Design→REVIEW→Build→Evolve | adr/0004:26 | `design.yml:69 next_stage: review`; `review.yml` exists; `.agent/ARCHITECTURE.md:16` spine lists REVIEW |
| 4 REVIEW phases (security/distributed/performance/cto) match agents+emits | adr/0004:29-36 | `review.yml` phases + 4 matching agent cards under `.agent/agents/` |
| New assets: `review.yml`, 3 reviewer agent cards, `ai-sdlc-review.md` skill card | adr/0004:49-54 | All present; `internal/adr.TestADR0004_*` also asserts this |
| Policy update: `modes.yml` adds `review: skip/standard/full/full` per-mode depth labels | adr/0004:55 | Verified in `.agent/policies/modes.yml` |
| Spine (`design.yml next_stage`) and `ARCHITECTURE.md` both updated for REVIEW | adr/0004:56-57 | `design.yml:69`; `ARCHITECTURE.md:16` |
| Machine-readable per-phase VERDICT + `review_status` metric drives orchestrator routing | adr/0004:84 | `converge.go:206,259 evalReviewStatus`; `parseReviewerVerdict` |
| D2/D3/D5/D6/O3/O4 (DECISIONS.md): CC-native orchestration; out-of-band gate as source of truth; v0 scope=Context+Harness; gate shells real harness, Opus floor, `forge evolve` unattended entry, convergence from `converge` pkg, agent phases real via `--executor command --agent-cmd claude`; url-shortener first vertical slice; `enforce: block` settled | .agent/DECISIONS.md:5-15 | `internal/gate/gate.go` shells real harness; `modes.yml router_floors`; `cmdEvolve`/`internal/converge`; `main.go --agent-cmd` default `claude`; `examples/url-shortener/`; `harness/policies.yml:8 enforce: block` |
| D1/D4 (Claude-only v1 routing scope: Haiku/Sonnet/Opus tiers) | .agent/DECISIONS.md:8 | `internal/routing/routing.go:20-22` only 3 tier constants, no vendor field |
| `required_gates` are mode-filtered front gates; only `agent:harness` is gate-only | build.yml harness/QA; evolve.yml `harness-gates` | `gatesFor` intersects with `ModePolicy.Gates`; serial/parallel engines run non-harness agents after green front gates; shipped-workflow tests prove Evolve `implement→harness-gates→review→evaluate` and red gates loop only to `implement` |
| `required_when: modes.yml#...` skips a phase when mode disallows | build.yml:78, evolve.yml:83 | `mode_gating.go:51-57 skipByMode`/`requiredWhenKey` |
| `on_fail: {action: loop_back, target_phase}` on a gate phase jumps back and re-runs | build.yml:67-69 | `orchestrator.go:343-358 loopBackTo`, bounded by `MaxLoopBack` |
| `on_fail.target_phase` on an agent/reviewer phase triggered by `REQUEST_CHANGES` | review.yml:51,72,101 | `runAgentPhase`→`AgentVerdict`→`loopBackTo`; live-validated via `forge validate --models` |
| `stop_condition.on_unmet: {loop_to_next_roadmap_item, target_phase}` | build.yml:112-114 | `loop.go:213-227 nextStartPhase`, unconditional across stop types |
| `uses_template` injects referenced `.ai/prompts/*.md` into the agent's prompt, validated | review.yml:44,67,94 | `prompt_context.go:327-343`; `doctor.EvaluateWorkflowModels`; live `forge validate --models` shows PASS for all 4 |
| `emits:` declares output artifacts consumed by downstream phases | build.yml:46, design.yml:49, discover.yml:36,50-51,62 | Prompt context injects prior emit content; the output contract requires every declared file to be contained, regular and non-empty, and provenance captures content/prompt hashes |
| Enabled `writes_adr` produces a fresh machine-verified ADR rather than narration-only | design.yml solution-architect | Condition/mode/lifecycle and target containment determine the effective write scope; command-mode pre/post snapshots require exactly one new canonical non-empty regular ADR for the current attempt and append provenance; disabled modes impose no ADR postcondition |
| `feeds_forward: true` remembers a phase's output for later phases | build.yml:41, evolve.yml:59 | `prompt_context.go:183 feedsForward`→`phaseOutputLedger`→`appendFeedbackLanes` |
| Evolve scan breadth is mode-sensitive, evidence-backed and durably resumable | ADR 0020; modes.yml `workflow_depth.evolve`; evolve.yml `scan_contract` | `internal/evolvescan` validates the exact final marker, six-dimension/depth rules, derived tasks and existing in-repo UTF-8 regular-file locators; `asset/scan_contract.go` binds the capability to the first readonly feed-forward phase; output-contract/prompt code preserves the full canonical report; checkpoint v3 restores scan plus integer budget/call/loop-back state, with serial no-replay and explicit iteration-only parallel recovery tests |
| `fresh_context: true` gives a reviewer a clean slate | build.yml, review.yml phases | `asset.go:127 FreshContext` makes `appendFeedbackLanes` a no-op |
| `model_tier:` per-phase override raises but never lowers the routed tier | build.yml:53,76; design.yml:32 | `routing.Higher(base, p.ModelTier)`; reviewer floor cannot be undercut |
| `optional_for: [mode,...]` skippable unless lifecycle floor maxes stage depth | discover.yml:44, review.yml:80 | `mode_gating.go:51-94 skipByMode`+`stageDepthAtMax`; tested incl. production-override edge case |
| `depends_on:` declares phase ordering for the opt-in parallel engine (mechanism; no shipped workflow uses it yet) | mechanism-only | `orchestrator/waves.go` topological sort; `--parallel` flag; `declaresDependsOn` keeps default serial |
| `stop_condition.type: human_gate` + `human_approval: required`, non-bypassable | design.yml:55-58 | `converge.go:137-177 IsHumanGate`/`humanGate`: only path to `met=true` is `sig.HumanApproved` |
| `stop_condition.type: conjunction`, `all_of` evaluated live per metric, unknown metric never silently passes | build.yml:101-110, discover.yml:71-78, review.yml:127-133 | `converge.go:183-213 Evaluate`/`evalOne` |
| `anti_pattern: round_count` — loop never terminates on iteration count alone | build.yml:111, evolve.yml:110, discover.yml:73 | Structurally enforced: `MaxIter` is a safety bound, reported distinct from "converged"; label itself is logging-only but the underlying discipline is real |
| `on_approved.next_stage` — approval unlocks the named next stage and has routing semantics only | design.yml, review.yml | `OnApproved.NextStage` drives report/chain/resume; produced files must be owned by phase `emits`/`writes_adr`, and `forge check` rejects unsupported `stop_condition.on_approved.emit` instead of silently dropping it |
| `stop_condition.type: external`, named triggers each independently real (no literal `Triggers` field, but every trigger works) | evolve.yml:104-108 | `human_pause` via SIGINT/SIGTERM context cancellation; `budget_exhausted` via `Engine.BudgetExhausted`; `no_gaps_found` via no-progress tripwire mapped to clean stop |
| `reviewer.md` binary `VERDICT: APPROVE`/`REQUEST_CHANGES` contract | .agent/agents/reviewer.md:31-45 | `parseReviewerVerdict` exact-matches both tokens; `TestParseReviewerVerdict_*` pass |
| `security-engineer.md` / `distributed-engineer.md` / `performance-engineer.md` binary `VERDICT:` contracts | respective cards | Same generic token-shape parser; each `review.yml` phase's `on_fail: loop_back` confirmed |
| `cto.md` design-stage role: doc-only proposal-generator, no VERDICT contract (correctly none declared) | .agent/agents/cto.md:1-32 | `design.yml` P2 has no `on_fail`/verdict field — matches |
| `cto.md` review-stage 5-way `VERDICT:` contract | .agent/agents/cto.md:63-96 | `parseExecutiveVerdict` matches all 5 tokens; `reviewStatus()` keys off literal phase name; tests pass |
| `product-manager.md` `CONFIDENCE: <N>` contract | .agent/agents/product-manager.md:32-52 | `parseConfidenceScore`; `requirementConfidence()`; `converge.go:247 evalRequirementConfidence`; end-to-end test passes |
| Build QA has an explicit strict report verdict in addition to its independent `test` gate | ADR 0014; `.agent/agents/qa.md`; `build.yml` P5 | `verdict_contract: qa_v1` cannot be mode-skipped, keeps `test` effective even when mode gate filtering would remove it, and targets an existing earlier writable/non-skippable implementer. It exclusively parses the byte-exact final non-empty `QA_VERDICT: ACCEPTED|REJECTED`; Claude requires one complete success envelope and process capture preserves raw whitespace. Missing/malformed output and generic reviewer tokens fail closed, `REJECTED` loops to implementer with source-neutral repair evidence, exhausted budget aborts, and dry/parallel bypasses are rejected |
| `architect`/`explorer`/`implementer`/`planner`/`researcher` cards correctly declare no machine verdict contract | respective files | Full-text grep confirms no `VERDICT:`/`CONFIDENCE:` claimed or missing |
| Agent-card text (incl. contract clause) actually injected into the agent's prompt at runtime | all contract cards | `prompt_context.go:467-476 readCard` + `buildPromptWithEmits:410` passes card verbatim |
| `.ai/prompts/{02,03,05,06}-*.md` templates carry the "7 Task" structure the cards describe; `09-cto-review.md` matches the "Five Questions" format | ai-sdlc-review.md:23-66 | `grep -c "^### Task"` = 7 for each; `09-cto-review.md` has 5 `#### Question N` headers |
| `cognitive-architecture.md` machine-checkable subset (layering/cognitive/package/fanin) + anti-pattern directory-name detector | skills/cognitive-architecture.md:30-36 | `arch-check.mjs` implements `checkLayering/Package/Fanin/Cognitive` + `checkAntiPatterns`; live run all PASS |
| `clean-architecture.md`/`modularization.md` — circular dependency = 0 via gate | skills:28,20 | `checkCircular` DFS cycle detection enforces `policies.yml:circular_dependency_count:0`; live PASS |
| `refactor-large-file.md` `max_file_lines`(500) / `project-reorganization.md` `max_root_files`(15) enforcement | skills:9, 6/9 | `harness/gate.mjs:75,97,113,115` |
| `testing.md` `coverage_min`(80%) threshold enforcement, honest N/A when no coverage tool installed | skills/testing.md:9 | `acceptance-quality.mjs:105-153` real framework-backed probe |
| `code-review.md`/`security-review.md` — pure playbooks referencing existing `gate.mjs`, no separate contract needed (correctly so) | full files | Neither declares a novel forge-core-invoked mechanism |
| `ai-sdlc-review.md` — all 10 `.ai/prompts/00-09-*.md` + `QUICKSTART.md` referenced files exist | skills/ai-sdlc-review.md:110-121 | `ls .ai/prompts/` confirms all 10 |
| Workflow-depth mode-gating for discover/adr/evolve dimensions | CURRENT_SPRINT.md:28 | `mode.go:102-105 DiscoverDepth/DesignDepth/ReviewDepth/ADR` all present, consumed by `mode_gating.go` |
| Audit gap ①-③ (loop-back control flow, adapter `test:` consumption, coverage-by-mode) closed | CURRENT_SPRINT.md:48 | `asset.go` OnFail/OnUnmet; `acceptance.mjs:180 appTestPlan`; `adapters.mjs:236 computeCoverageThreshold` |
| Audit gap ⑤ (`workflow_depth.discover/design/adr`, `model_tier`) modeled | CURRENT_SPRINT.md:48 | `mode.go:102-105`; `engine_build.go:225-234 Higher()` raise-only |
| `coverage_delta`/`enforce_floor` honesty annotations resolved | CURRENT_SPRINT.md:47 | `adapters.mjs:238`; `gate.mjs:8-127 resolveEnforce`, tested |
| Orchestrator loop-back state machine real and unit-tested (real-LLM recovery rests on general ignition capability, not loop-back-specific) | CURRENT_SPRINT.md:51 | `loopback_test.go`; no explicit real-claude gate-FAIL→loop-back→recovery transcript documented, but mechanism itself is sound |
| Discover/ADR decision logic (skip/depth/ADR-required) is real Go code, independent of agent quality | CURRENT_SPRINT.md:61 | `mode.go` |
| `avg_cost_usd` now real billing via `claude --output-format json total_cost_usd`, not list-price estimate | CURRENT_SPRINT.md:74 | `cost.go`; live value cited `avg_cost_usd=0.1841` |
| `RequirementConfidence` and `FileDelta` `converge.Signals` — both declared-and-unassigned gaps closed (the latter had been an active false-positive bug) | CURRENT_SPRINT.md:130,139,140,144 | `gates.go:74-75,299`; test-verified via fake-agent tokens and real git fixtures |
| SCA/CVE `dependency_vulnerabilities` now a real PASS/FAIL scan, not a permanent N/A — moved from BLOCKED-EXTERNAL 2026-07-16 | CURRENT_SPRINT.md (Sprint 32) | `.agent/security/advisories.json` contains four OSV records fetched on 2026-07-16 for this repo's one actual dependency, PyPI:PyYAML; `forge accept` now reports `[PASS] dependency_vulnerabilities`. The gate reads only this committed snapshot and is deterministic/offline. Current 2026-07-27 DNS restrictions may prevent an immediate refresh; they do not turn a successful historical fetch into live network availability. Honest scope: only ecosystems/dependencies present today are evaluated against real data. |

---

## BLOCKED-EXTERNAL (no active items; historical resolutions)

| Requirement | Source | Evidence |
|---|---|---|
| ~~Firecracker microVM sandbox prerequisite~~ — **RESOLVED 2026-08-05, moved to DONE** | CURRENT_SPRINT.md Sprint 66–71 | Firecracker v1.7.0 booted a real KVM microVM and the runner is wired into the Go executor. Full coding-workspace exchange and stronger container hardening remain separate deferred work, not an external-resource block. |
| ~~Cross-vendor LiteLLM host validation~~ — **RESOLVED 2026-08-05, moved to DONE** | CURRENT_SPRINT.md Sprint 66–67 | DeepSeek and local Ollama were both routed through the local LiteLLM gateway; one backend completed inference and the other reached its intended upstream before an account limit. A production provider pool remains deferred by design. |
| ~~SCA/CVE vulnerability scanning needs an OSV/NVD advisory database~~ — **RESOLVED 2026-07-16, moved to DONE** | CURRENT_SPRINT.md Sprint 32 | See DONE bucket: a real OSV snapshot was fetched and committed on 2026-07-16. Acceptance stays offline; current DNS restrictions affect refresh operations, not the deterministic scan of the existing snapshot. |

---

## DEFERRED-BY-DESIGN

| Requirement | Source | Evidence |
|---|---|---|
| Independent `agent-os` submodule repo for global governance sharing, incl. mechanism/split-boundary/Stage A-B rollout/trigger condition | ROADMAP.md:26; adr/0003 (whole document) | `docs/adr/0003:1-8`: status Proposed, "用户定位置+批准前，不创建任何仓、不改任何代码" — explicit textual hold pending user decision on every sub-clause (mechanism, path rules, rollout stages, trigger condition) |
| Budget governance (PDP-style policy engine), materially broader than the run-level dollar cap already built | ROADMAP.md:29; north-star.md:44 | Listed v3; described as a distinct OPA/Rego "Policy/Gov" service, not started |
| Web UI (v3) | ROADMAP.md:29,33; ARCHITECTURE.md:52,57; DECISIONS.md D1 | `forge-web`/Web-UI is in the target architecture and v3 roadmap; zero frontend code exists in the current phase, so this is deferred rather than architecture-external |
| Gateway, Knowledge-Engine unbuilt roadmap items | ARCHITECTURE.md:45 | Gateway and complete Knowledge-Engine remain roadmap items; no `gateway`/`knowledge` package exists |
| Complete coding-workspace sandbox exchange and hardening | ARCHITECTURE.md:41,45; CURRENT_SPRINT.md Sprint 83 | Docker and Firecracker command runners now exist. The shared interface still lacks a repository snapshot/mount, scoped credential channel and declared artifact sync-back; Docker root/read-only-filesystem/capability/seccomp hardening also remains explicit follow-up work. |
| Remaining production runtime capabilities behind `forge-runtime` | adr/0002; adr/0006–0013; adr/0015; adr/0017–0019; adr/0021–0036 | Local Project Run persistence, bounded history/writeback, opt-in Responses, Group dossiers/snapshots/receipts/analysis/panels/synthesis, strict Hub schema ownership, durable Agent Graph/Core Plan, scheduled multi-node candidates, per-node request/lifecycle storage, predecessor receipt/content disclosure with independent consent, wave-ready/admission planning, scheduled-family pid-sidecar adjudication and legacy-v4 no-send operator adjudication (see the 2026-08-08 Resolution addendum) now exist. A top-level whole-Graph execution loop, automatic concurrent wave execution/failure propagation/recovery, Project Run resume/branching, remote account/sync, shared ACL, mutating tools, specialist discussion, production multi-provider routing and live Group multi-Agent execution remain explicitly deferred. |
| harness CLI future consolidation into a Go static binary, zero runtime deps | adr/0002:14 | "未来" (future) is explicit future-tense wording in the Decision text itself; `harness/*.mjs` still Node.js today |
| HA/distributed approval-wait backend (Temporal or equivalent), beyond the delivered local durable chain state | adr/0002; north-star architecture | Local `chain-state.v2` resume and private approval markers are DONE; a multi-node scheduler/database-backed wait service remains future infrastructure work |
| Approval-time repository-truth materializer | design workflow history | Current `on_approved` is intentionally routing-only. A future materializer first needs an adopted producer/source mapping, freshness rules and atomic commit contract; shipped assets no longer claim `.agent/*`/root `project.yml` are synthesized, and governance rejects `on_approved.emit` |
| `priorities` (speed/quality/cost) independent routing-weight semantics, deliberately not wired | CURRENT_SPRINT.md:48,68 | `route.go:97-102` comment: effect is already carried upstream; an independent weighting scheme is "deliberately not wired" — explicit anti-gold-plating rationale |
| `risk.FromChangedPaths` stays a coarse path-substring heuristic (no AST/call-graph, `ProdTraffic` never inferred, `--from-git` sees only tracked changes) | CURRENT_SPRINT.md:34 | `risk_diff.go:3-28` header: "Precise extraction needs real signal: AST/call-graph… that is v3" |
| Recursion-depth guard (`FORGE_AGENT_DEPTH`) scoped to accidental recursion only, not adversarial env tampering | CURRENT_SPRINT.md:77 | `command_executor.go:261-269`: explicit comment states hardening to fail-secure "would buy no real safety" — a deliberate, reasoned non-goal |
| Semantic/embedding-based context retrieval (vs current TF-IDF) | CURRENT_SPRINT.md:155 | `prompt/retrieve.go:19-20`: "true semantic retrieval is v3 work… needs an external embedding model"; sprint explicitly frames building it now as gold-plating |
| Full multi-vendor "Discover" v3 label vs. the (already largely built) 3-phase Discover mechanism — the mechanism itself is DONE; only the cross-vendor/v3 framing is deferred | ROADMAP.md:29 | See GAP row below for the one real sub-contract still missing inside Discover |

---

## GAP

> Historical finding table: `CONFIRMED` below describes what the 2026-07-02
> audit observed at that time. It is not a current defect list. Present status
> is the 2026-07-27 closure matrix above plus the resolution addendum below.

Each row below was independently re-verified against current source as part of this merge. Where
source reports disagreed, the disagreement is resolved explicitly rather than concatenated.

| Requirement | Source | Verdict | Evidence |
|---|---|---|---|
| **Discover's `requires_tools: [web_search, web_fetch]` anti-hallucination contract — "degrade to advisory + flag if absent"** | discover.yml:44-45,48; ARCHITECTURE.md:10 | **CONFIRMED** | Repo-wide grep for `web_search`/`web_fetch`/`requires_tools`/`RequiresTools` returns zero hits in `forge-core`; `asset.Phase` (asset.go, confirmed by direct read for this merge) has no `RequiresTools` field at all, so the YAML key is silently dropped on decode. No degrade-or-tag logic exists anywhere. Needs no external resource — pure Go control flow (check whether the agent's allowed-tools list includes a search tool, tag the phase output). This is the single most actionable gap in the whole audit: it is the one place the SCA/OSV honest-N/A pattern *should* apply but doesn't, and it is the one un-modeled sub-contract keeping the otherwise-DONE Discover mechanism from being a full, honest v1 (not a v3-blocked item). |
| **`mode_gating:` top-level workflow block (`gate_set`, `reviewer_required`, per-mode depth table referencing `modes.yml#...`) is never decoded** | all 5 workflow files, e.g. build.yml:25-34, discover.yml:19-24 | **CONFIRMED** | Directly re-verified for this merge: `grep -n "ModeGating\|mode_gating" forge-core/internal/asset/asset.go forge-core/cmd/forge/*.go` → zero hits. The block survives YAML→JSON transcode but is silently dropped by `encoding/json` on decode into `asset.Workflow`. The equivalent *behavior* is real (via `internal/mode`'s independently hand-maintained Go mirror of `modes.yml`), so there is no functional loss today, but the field itself is a dead, unconsumed declaration and `internal/yamlpath` (built specifically to resolve `file#dot.path` refs like this one) is called from zero non-test files — a built-but-unused capability. |
| **`readonly` (phase/workflow level) is never decoded, and no enforcement exists — reviewer phases get identical write permissions to implementer phases** | every phase in every workflow, e.g. build.yml:17,39,51 | **CONFIRMED** | Directly re-verified: no `Readonly` field in `asset.Phase`/`asset.Workflow`. `--executor=command` passes the same `--permission-mode acceptEdits`/`--allowedTools` to every phase regardless of declared readonly status (`engine_build.go:122-126`, `bindRunOpts`). A `readonly: true` reviewer phase can write to the working tree exactly like an implementer. |
| **Gate phase `blocking: true` field is never decoded** (the behavior it names is nonetheless real via unconditional default logic) | build.yml:63, evolve.yml:71 | **CONFIRMED, downgraded severity** | No `blocking` field on `asset.Phase` (verified, zero hits). However a gate phase with no `on_fail` (or a spent loop-back budget) aborts the run by default regardless — so the *documented behavior* is functionally met without ever reading this field. This is a dead/redundant declaration, not a missing capability; lower priority than the `readonly`/`mode_gating` rows above. |
| **`secondary_template` (review.yml's `performance-reliability-review` second AI-SDLC template) is entirely unconsumed** | review.yml:95 | **CONFIRMED** | Zero occurrences of `SecondaryTemplate`/`secondary_template` anywhere in forge-core; not a struct field, not read by prompt-building, not checked by `doctor.EvaluateWorkflowModels`. Live `forge validate --models` run produces no PASS/WARN/FAIL line for it at all, unlike every `uses_template` reference which does — asymmetric validation coverage. Trivial fix: mirror the existing `UsesTemplate` field/consumer pair. |
| **`stop_condition.on_rejected` is dead code for both shapes it's declared for** — (a) the `human_gate` shape (design.yml) is unreachable because `forge evolve` hard-refuses any human_gate workflow before ever entering the loop, and `forge run` never loops at all; (b) the `conjunction` shape (review.yml) is unreachable because the same handler only fires inside an `IsHumanGate` branch | design.yml:59-61; review.yml:134-137 | **CONFIRMED via direct re-read for this merge** | `evolve.go:65-67 rejectHumanGate` fails closed before `execLoop` is ever reached for `converge.IsHumanGate(wf.Stop)==true`; `main.go:293-295 cmdRun` calls single-pass `eng.Run`/`eng.RunParallel`, never `LoopEngine`. So `loop.go:225-239 nextStartPhase`'s `OnRejected` branch (`converge.IsHumanGate(l.Stop) && l.Stop.OnRejected != nil`) is provably unreachable from any real CLI path for the human_gate case — confirmed by tracing every caller. For review.yml's `type: conjunction` stop, `IsHumanGate` is false by construction so the same branch never evaluates there either; the code happens to coincide with review.yml's declared target (phase index 0 = `security-review`) via the `return 0` fallthrough, masking the gap in practice, but the field's *value* is never actually read — changing `target_phase` to a non-first phase would silently do nothing. Notably the code's own comment cites this as closing "the missing state-machine edge from asset-runtime-gap.md §3.1," but the fix was applied one layer too deep (inside `nextStartPhase`) without also removing/documenting the outer guards that make it unreachable — the mechanism was built without checking it could ever run. Zero test coverage confirms this: `grep -rn OnRejected` finds it in exactly one non-test file. |
| **`review.yml`'s `required_when` annotation on `security-review` is decorative — zero Go consumers** | review.yml:48 | **CONFIRMED, low severity** | `grep -rn "required_when\|RequiredWhen"` across `forge-core/**/*.go` returns zero matches; the actual skip logic runs entirely through `mode_gating.go`'s stage-level `skipByMode`, making this specific phase-level annotation vestigial/superseded rather than broken — harmless but literally unaddressed, as `CURRENT_SPRINT.md:117` itself already admitted. |
| **yaml2json silently drops a bare `-` sequence item when its nested value is nil** | CURRENT_SPRINT.md:117 | **CONFIRMED, dormant** | Directly re-verified for this merge: `forge-core/internal/yaml2json/sequence.go:56-68` — `parseSeqItem`'s empty-item branch (`seqItemEmpty`) only appends `if item != nil`; every other branch always appends, so this is a genuine, asymmetric semantic gap. Zero real-repo `.agent/*.yml` files hit it today (none use a bare `-` null item), so it is a live but currently-dormant correctness bug, not an active failure. |
| **`confidence_metric:` field is declared-but-inert** — the confidence-parsing mechanism works, but via a hardcoded "any phase producing `CONFIDENCE: N`" pattern match, not by reading which phase declared the field | discover.yml:37 | **CONFIRMED, mixed** | `Phase.ConfidenceMetric` (asset.go:129) is decoded but `grep -rn "\.ConfidenceMetric\b"` returns zero read sites. Downgraded from a pure gap: the *outcome* the field describes is real and tested (`parseConfidenceScore`, `evalRequirementConfidence`); only the specific field-driven dispatch is unbuilt. Same shape as the `Triggers`/`AntiPattern` rows — mechanism real, declared field itself unread. |
| **G3: multi-dimensional model routing (complexity/dependency/context/business-impact) did not drive real agent execution — only the standalone `forge route` CLI** | PROJECT.md:13; ARCHITECTURE.md:47-50 | **RESOLVED (Sprint 63)** | `routing.FromChangedPaths` now derives the execution dimensions from the actual changed-path set. `resolveAutoDims` supplies them to both run and evolve, and their shared `phaseTierResolver` applies `routing.Score` through `scoreAdjustedTier` before the budget adjustment. The escalation is raise-only; explicit phase floors and the later safety/budget rules remain authoritative. The historical finding no longer describes current source. |
| **REVIEW mode-gating table's literal "balanced runs only P1+P2" claim is false** — balanced mode actually runs P1, P2, *and* P4 (only P3 is conditionally skippable) | adr/0004:41-45 | **CONFIRMED via direct re-read for this merge; resolves a cross-report disagreement** | `review.yml:48,80` (re-read directly): `security-review` (P1) carries `required_when` but no mode-restriction beyond that; `executive-review` (P4, cto-synthesis) carries **no** `required_when`/`optional_for` at all, so it always executes; only `performance-reliability-review` (P3) has `optional_for: [balanced]`. `orchestrator_review_gating_test.go:186-203 TestRun_BalancedSkipsOptionalReviewPhase` (re-read directly) explicitly asserts `security-review, distributed-review, cto-synthesis` all execute under balanced and only `performance-review` is skipped — i.e., 3 of 4 phases run, not "P1+P2" as ADR-0004's Decision text literally states. This is a genuine textual/behavioral mismatch in the ADR itself, not a code bug — the *code* is deliberately more conservative (runs the executive synthesis phase always) than the shorthand prose describes. Classified GAP against the ADR's literal claim; the actual system behavior is arguably safer than what was promised, so treat this as a documentation-accuracy gap, not a missing-capability gap. |
| Target stack: `forge-ai`=Python (intelligence layer) — no directory, no code, and (unlike Rust) no explicit version-tag deferral text | adr/0002:11 | **CONFIRMED, unexcused** | No `forge-ai/` directory anywhere (`find` confirms); the ADR's Decision text gives Rust an explicit "v3" tag but gives this component no equivalent deferral language — genuinely unexcused by the source, though clearly not urgent (no workflow currently depends on a Python intelligence-layer service). |
| Target stack: `forge-web`=TS/Next (UI) — no directory, no code, no explicit deferral text at the ADR-0002 level (though `ARCHITECTURE.md`/ROADMAP.md elsewhere do call Web UI "仍为路线图", making the *overall* Web UI item DEFERRED-BY-DESIGN at the project level) | adr/0002:11 | **DOWNGRADED — effectively subsumed** | Same absence as forge-ai, but this one has a real deferral trail elsewhere in the project's own sources (ARCHITECTURE.md:45, CURRENT_SPRINT.md:156). Listed here only because ADR-0002 itself doesn't carry the deferral text; the overall Web UI capability is already correctly tracked as DEFERRED-BY-DESIGN above. Not a separate actionable item. |
| "Go 在 v2 进" clause bundled Temporal/Postgres/NATS into the *same* v2 timing as Go itself; Go shipped, the durable-infra trio did not | adr/0002:13 | **CONFIRMED, low severity** | Go landed (DONE, see above). `CURRENT_SPRINT.md:25` self-labels `durable_wait(Temporal)` "诚实标注 v2/v3" — i.e. the project's own honesty note already treats it as later than plain "v2", so in practice the project has already informally corrected this ADR-text imprecision; listed for completeness, not urgent. |

---

### Note on doc-drift-only items (excluded from the buckets above as non-functional)

The original audit found stale counts in `.agent/ROADMAP.md` (agents, skills,
workflows, checks and Go packages) plus an obsolete claim that the Python YAML
shim was primary. Those were documentation-only findings, not missing runtime
capabilities. The roadmap was corrected on 2026-07-27 to prefer stable
capability descriptions over brittle counts; the native Go YAML parser is the
primary path and Python remains a compatibility fallback.

---

## Resolution addendum (2026-07-02, same audit cycle)

Every `GAP` row above was triaged and acted on the same day this audit ran. Status per row:

| GAP row | Resolution |
|---|---|
| `requires_tools` degrade-and-flag | **RESOLVED.** `Phase.RequiresTools` decoded; `requiresToolsGuard` (cmd/forge/prompt_context.go) degrades honestly (dry-run / non-claude / no-allowlist / unconfirmed tool → advisory notice injected into the agent's prompt + logged) and confirms silently when tools ARE confirmed available. Live-wired into the real `agentExecutor` build path; unit + end-to-end tests added. |
| `mode_gating:` top-level block never decoded | **RESOLVED (2026-07-03, second pass).** Re-examined after further review: a comment alone left the cross-reference free to silently drift from `modes.yml` with nothing to catch it. Built a real drift-guard instead — `harness/check.py`'s new `check_workflow_mode_gating` (factored into `harness/mode_gating_check.py` for line budget) parses each workflow's `mode_gating:` block, resolves its `authority:` pointer's dimension, and compares every declared per-mode value against `modes.yml`'s canonical `workflow_depth.<dimension>`. Registered in `CHECKS`; runs clean against the real repo today (confirmed independently: zero issues), and would now catch future drift instead of silently tolerating it. `forge-init`'s `COPIED_FILES` manifest updated so scaffolded projects inherit it (a first pass missed this and broke `forge accept`'s scaffold-drift test; caught and fixed same-day). |
| `readonly` never decoded/enforced | **RESOLVED (2026-07-03, second pass; current-contract correction 2026-07-27).** The historical implementation used path-scoped Edit/Write deny/re-open rules. Current Claude integration instead runs readonly phases under `dontAsk`, with explicit pre-approved `Edit(...)` paths; it never emits deprecated `Write(path)`. Release phases are narrower still: `--tools Edit,Write` exposes the built-ins, `Bash WebFetch WebSearch` are denied, and only exact absolute `Edit(/<phase.emit>)` rules are granted. A whole-tree postflight rejects undeclared release changes. Ordinary readonly roles retain their validated per-agent directories. Unit/fake-agent tests prove argv and filesystem contracts; no new paid Claude run was authorized. |
| `blocking: true` field never decoded | **Left DOCUMENTED — deliberately, with reasoning, not by default.** Re-examined for a genuine implementable difference (unlike the two rows above): `blocking: false` is never used anywhere in any real workflow file (grepped to confirm), and the field's only alternative meaning (\"a failing gate does NOT abort the run\") would be new, untested, never-exercised behavior invented for a value nothing declares — implementing it would be gold-plating, not gap-closing. The one-line `NOTE:` comment stands as the correct final answer, not a placeholder. |
| `secondary_template` unconsumed | **RESOLVED.** Decoded; consumed at every site `uses_template` is (prompt injection in the new `cmd/forge/prompt_artifacts.go`, `doctor.EvaluateWorkflowModels` validation). Live-verified: `forge validate --models` now emits a PASS line for `review.yml`'s `secondary_template` reference, symmetric with `uses_template`. |
| `stop_condition.on_rejected` unreachable dead code | **RESOLVED (2026-07-03, current lifecycle correction 2026-07-27).** Built a minimal, bounded rejection loop-back for `forge run`: `.forge/<stage>.rejected` plus an actionable `on_rejected` resolves the target phase and starts there instead of phase 0. The marker is retained when rework fails and consumed only after rework completes successfully, so failure remains retryable without fabricating approval. Tests cover both retention and successful consumption; a later run after successful consumption falls back to phase 0. |
| `review.yml`'s decorative `required_when` | **RESOLVED (2026-07-03, second pass).** Re-examined: this field never did anything (confirmed dead via full-repo grep) and the honest fix is to remove the false declaration rather than build a phantom consumer duplicating the already-correct stage-level gating. Removed from `review.yml`; confirmed no test asserted on its presence, YAML still parses cleanly, `check.py` still passes. |
| yaml2json bare `-` sequence item dropped | **RESOLVED.** `parseSeqItem`'s empty-item branch now always appends (matching every other branch, including literal `null`), fixed and tested; confirmed zero effect on all 7 real repo YAML files (none use the pattern). |
| `confidence_metric:` field declared-but-inert | **RESOLVED (2026-07-03, second pass).** Re-examined: field-driven dispatch has real generalization value (any future phase can declare `confidence_metric: requirement_confidence` without new Go code), and `gatherSignals` already had the workflow in scope — low-risk to wire properly instead of leaving a comment. `requirementConfidence` now scans `wf.Phases` for whichever phase declares the matching `ConfidenceMetric`, falling back to the historical hardcoded phase name only when no phase declares the field (byte-for-byte behavior-preserving for discover.yml's current shape — verified by a dedicated regression test — with a second test proving a differently-named phase is now correctly picked up). |
| G3 multi-dimensional routing not driving real execution | **RESOLVED in Sprint 63.** `routing.FromChangedPaths` derives the dimensions, `resolveAutoDims` feeds real run/evolve boundaries, and `phaseTierResolver` applies the weighted score through a raise-only `scoreAdjustedTier` before the budget adjustment. The standalone `forge route` command is no longer the only consumer. |
| ADR-0004 "balanced runs only P1+P2" factual error | **RESOLVED.** Inline `[corrected 2026-07-02: ...]` erratum added citing the real behavior (P1+P2+P4 run; only P3 is conditionally skippable) and the test that proves it (`TestRun_BalancedSkipsOptionalReviewPhase`). Historical Decision text left otherwise intact. |
| `forge-ai`=Python missing deferral text | **RESOLVED.** Parenthetical deferral note added to ADR-0002's target-stack table, mirroring the Rust row's convention. |
| `forge-web` missing deferral at ADR-0002 level | **No action needed** — already correctly tracked as DEFERRED-BY-DESIGN via ARCHITECTURE.md/CURRENT_SPRINT.md; not a separate actionable item (per the original audit's own downgrade). |
| "Go 在 v2 进" ADR-0002 imprecision (Temporal/Postgres/NATS bundled with Go's timing) | **No action needed** — low severity, already informally superseded by CURRENT_SPRINT.md's own "v2/v3" honesty label on `durable_wait`. |

### Resolution addendum (2026-08-08) — DEFERRED-BY-DESIGN row "Remaining production runtime capabilities behind forge-runtime"

| Deferred item | Resolution |
|---|---|
| Safe hard-crash adjudication (v4 family) | **RESOLVED.** The scheduled family shipped first via ADR-0034 (pid-sidecar liveness); the legacy v4 family fence is now closed by `group graph run dispatch adjudicate` (no-send, operator-invoked, pinned Core with `hard_crash` support): the stranded `dispatch_unknown` claim is terminalized to a deterministic `failed_uncertain` state through the single terminalize CAS (`BEGIN IMMEDIATE`, `reject_terminal_replay`, guarded `transition_run`, exact-DELETE lane release), releasing the Project lane. No provider request is ever sent (constructor takes no provider factory and no credential source — type-level no-credential), wrong bodies are refused pre-bridge by a claim-digest cross-check, an old Core is refused at decide with a re-pin hint, and re-entry on any non-stranded claim is refused with zero mutation. Rust `HardCrash`/Go `hard_crash` classification lockstep with closed-world test extensions (A-hardcrash + scheduled-family fence). Verified: full workspace tests, `cargo clippy -D warnings`, Go graphterminal suite, `forge accept` gate. |

### Final closure note (2026-07-03)

Two rows above (`readonly`, `stop_condition.on_rejected`) are RESOLVED at the implementation level — both mechanisms are real, tested against their documented/simulated contracts, and independently re-verified in this repo — but their very last mile (empirical behavior under a real, paid `claude` subprocess) was explicitly deferred. This was put to the user directly rather than assumed either way: authorize a small budget-capped live `claude` run to close that last mile, or accept the current unit/simulation-tested state as sufficient. The user chose the latter — **explicit, informed sign-off that this session's scope is complete**, not an unresolved gap left hanging. Re-opening it in the future needs a fresh budget authorization, not a re-litigation of whether the mechanisms themselves are "done."

All fixes independently re-verified (not just trusted from the implementing agents' self-reports): `go build/vet/test -race` clean, `node harness/gate.mjs` PASS, `node harness/arch/arch-check.mjs` 8/8 PASS, `python3 harness/check.py` PASS, `node harness/acceptance.mjs` → **ACCEPTED**, plus live reproductions of the `secondary_template` validation line and the yaml2json bare-dash decode fix.

### 2026-08-13 portable Knowledge Graph Curation closure

The `knowledge-graph-curation` narrow package slice is closed only for source distribution of the two frozen ADR-0065/0066 exact-request partial projectors. The parent package rollout and all Wave 2 multi-surface extractor, coverage/staleness, full impact/cost/risk and authority work remain open; no new wrapper ABI, runtime route, live capture, persistence or impact claim was introduced.

### 2026-08-13 portable Change Impact Cost Risk closure

The `change-impact-cost-risk` narrow package slice is closed only for source distribution of the frozen ADR-0062 exact-request lexical ImpactPreScan projector. System impact remains UNKNOWN and the parent package rollout plus all Wave 2 multi-surface extractor, coverage/staleness, full Impact/Cost/Risk/materiality/safety and authority work remain open; no raw graph wrapper, runtime route, live capture or persistence claim was introduced.

### 2026-08-14 WorkIntent v1 Proposed candidate evidence

ADR-0077/0078 and Registry v32 record only exact Python/Go/Rust structural parity, pinned Schema/golden identity, a checker-only shadow and a source-only Python distribution boundary. This checked evidence does not accept semantic authority, close G0, authenticate origin/requester/owner, resolve references, assess freshness/materiality/scope, create a route, runtime, evaluator, producer, consumer, Run, RunJournal, lifecycle, Approval, Grant, persistence or effect. The `change-intake-orchestration` parent package and its remaining 31 items stay open.

### 2026-08-14 Authenticated ADR approval v1 Proposed structural prerequisite evidence

ADR-0079/0080 and Registry v33 record only caller-supplied structure/digests/relations, pinned Schema/golden/proposal identity, one checker-only shadow and dependency-free Python source-only distribution. This checked evidence does not verify Ed25519, authenticate or authorize, sign or issue a receipt, consume or prove an external root pin, trusted time or revocation currentness, provide CAS/durability/Accepted lifecycle, close G0, add a Skill, route, scope/evaluator/producer/runtime, persist or effect. A future Go service, production keys and state are not copied; full authenticated approval, ADR lifecycle and package rollout remain open.

### 2026-08-14 Authenticated ADR lifecycle v1 Proposed candidate evidence

ADR-0081 is independently reviewed Catalyst-only Go approval-authority evidence; ADR-0082/0083 and Registry v34 distribute only a dependency-free Python lifecycle structural candidate with exact core pins and one checker-only shadow. The complete scope SHA-256 remains `8ba82b638e8031f0d1be2b9ea6d522a4b9cf064a4ed532e1f0d3281f2dfe874c`; no Skill, route, evaluator, producer or runtime is added. Generated projects receive no Go authority, production key or state. Structural success, an approval prerequisite or `forge accept` does not perform source mutation, lifecycle acceptance, atomic durable publication, compliance, permission, effect or G0 closure; full authority-bearing lifecycle and package rollout remain open.

### 2026-08-14 Authenticated ADR lifecycle authority evidence and source-only governance

ADR-0084 freezes exact44 Catalyst-only Go lifecycle authority evidence; ADR-0085 and Registry v35 record its opaque `StoredAuthorization`, independent trust, signature, atomic transition, durable CAS and historical replay boundaries without distributing the implementation. The full scope digest and one lifecycle checker-only shadow remain unchanged. Generated projects receive only exact4 decisions and governance source, no Go, production root/key/seed/state/receipt/ledger, route, Skill, service or runtime profile. This evidence does not generate Accepted source, grant permission, attest compliance or rollback resistance, create a generalized Effect or close G0.

Legacy Memory/ADR unverified read-only import shared governance is recorded by ADR-0086/0087 and Registry v36 without expanding the full scope digest. The checker-only shadow exposes only its real zero-argument argv; operator-supplied stdin/EOF remains outside detector metadata. Python source distribution excludes Catalyst-only Go parity, ambient repository reads, database/state, route, Skill, runtime, persistence and authority. The exact18 fresh and real Registry v35 upgrade scaffold closure is complete, and the implementation-roadmap checkbox is promoted.

Kernel operational reference shared governance is recorded by ADR-0088/0089 and Registry v37 without expanding the full scope digest. The checker-only shadow executes exactly `[python3, harness/kernel_operational_contract_check.py, --golden, .]`; exact18 distribution copies only the dependency-free Python core and governance. Catalyst exact11 Go/exact13 Rust module parity and the shared `lib.rs` unique operational registration remain excluded from generated projects; `lib.rs` is not whole-file pinned. Route, Skill, authority, persistence and effects remain excluded. Fresh generated core 33/skip2, governance 12/skip2, real Registry v36 added18/changed34/second0, and older v35/v34/v33 inverse upgrades are closed. This is only the operational-reference subclosure: the full Kernel ABI roadmap parent remains open.

Kernel decision reference shared governance from ADR-0090/0091 is delivered in Registry v38 without expanding the complete scope mapping or digest; both ADRs remain Proposed. The checker-only shadow executes exactly `[python3, harness/kernel_decision_contract_check.py, --golden, .]`; exact19 distribution copies only the dependency-free Python core and governance. Catalyst exact13 Go, flat exact9 Rust and the shared `lib.rs` unique decision registration remain excluded from generated projects; `lib.rs` is not whole-file pinned. All 22 attestations remain false, declared authority/hardness stays ineffective and instructions stay disabled. No route, Skill, runtime, PDP or controller is added. The structural reference-family item passed formal `forge accept` and is complete; ADR-0038 remains ADOPTED-PARTIAL, while DecisionCapsule, AuthorizedTransactionSpec, authenticated PDP and the rolling controller remain open.

Decision Capsule structural replay Candidate governance from ADR-0092/0093 is recorded in Registry v39 without expanding the complete scope mapping or digest; both ADRs always remain Proposed/null. The checker-only shadow executes exactly `[python3, harness/decision_capsule_contract_check.py, --golden, .]`; exact19 distribution copies only dependency-free Python core/governance. Catalyst exact15 Go, exact14 Rust and shared registration stay excluded. All 32 attestations, replay controls and seven completion claims remain false; there is no route, Skill, runtime, Reflection consumer, persistence, PDP or controller. The narrow item remains pending independent review, scaffold evidence and formal `forge accept`; ADR-0038, complete DecisionCapsule and AuthorizedTransactionSpec remain open.
