{
  "name": "rules",
  "cases": [
    {
      "id": "frontend_builtin_rules",
      "input": "用 Flutter 实现 ERP 排程列表页",
      "assert": {
        "domain": "frontend_ui",
        "tier": "standard",
        "risk": "low",
        "has_rule": ["visual-core", "system-type-optimization"]
      }
    },
    {
      "id": "generic_methodology_only",
      "input": "重构 Python 配置加载代码",
      "assert": {
        "domain": "generic",
        "has_rule": ["system-type-deterministic"],
        "lacks_rule": ["visual-core"]
      }
    }
  ]
}
