# ForgeOS architecture rules (machine-readable) — ENFORCED by harness/arch/arch-check.mjs.
#
# ForgeOS's own load-bearing wall, applied to ForgeOS. Until now the
# clean-architecture skill only DECLARED dependency direction; nothing checked
# it. This file makes the rules executable: arch-check.mjs reads it and FAILS the
# build on any violation (layering / package size / fan-in / cognitive load /
# anti-pattern naming). Two further checks — function-length and circular-
# dependency — are ALSO enforced by arch-check.mjs but read their limits
# (max_function_lines, circular_dependency_count) DIRECTLY from
# harness/policies.yml, so they are deliberately NOT mirrored here: a single
# source has nothing to drift from. (function-length fails closed if that limit
# is missing; circular_dependency_count is a fixed 0 — any cycle fails.)
#
# Every numeric limit below is set to ForgeOS's MEASURED actual + sensible
# headroom, so the repo PASSES honestly today and only trips on real drift.
# Measured 2026-06-19 (node harness/arch/arch-check.mjs prints the live numbers):
#   root:      4 source dirs, 2 root files
#   package:   max 11 code files/pkg (forge-core/cmd/forge); max EXPORTS/pkg = 25
#              (orchestrator), then 23 (mode's gate/depth vocabulary) — RECALIBRATED
#              2026-06-27: the prior "11" was undercounted by a countGoExports bug
#              that skipped grouped `const (...)` blocks (now fixed in scan.mjs), so
#              the export budget moved 20 → 30 to match the corrected real ceiling +
#              headroom. The scaffold/upgrade tooling lives in its own
#              harness/scaffold/ sub-package, so harness/ stays a thin gate package.
#              RECALIBRATED 2026-07-02: forge-core/cmd/forge's gates.go was pressed
#              against file.max_lines (500) by the RequirementConfidence + FileDelta
#              gap-fix (two more Signals wired end-to-end, mirroring the already-
#              landed ReviewStatus wiring) — "先拆分,再继续" split it into gates.go
#              (orchestration) + gate_resolve.go (the N/A exemption matrix + per-gate
#              resolution) + approve.go (the `forge approve list` CLI, previously
#              living at the tail of gates.go), raising the file count 14 → 16 and
#              (on review, wrongly) pushing max_files 14 → 18. gate_resolve.go was
#              actually pure business logic — gate-name -> tri-state verdict
#              resolution over the converge/gate types only, no CLI concerns — so it
#              moved OUT of cmd/forge into internal/gate/resolve.go instead (extending
#              the existing gate.Result/ProbeAll package, mirroring the
#              internal/attribution precedent), dropping the count back to 15 (14
#              original + approve.go, a genuinely new CLI surface with nowhere else
#              to go). max_files recalibrated 18 → 16 (actual 15 + headroom 1,
#              matching this metric's original 13-actual/14-budget convention) rather
#              than left at the wider 18.
#              RECALIBRATED again 2026-07-02: the secondary_template consumption gap-fix
#              split prompt_context.go's artifact-context lane (emitsContext/
#              usesTemplateContext/secondaryTemplateContext/appendArtifactContext) out
#              into prompt_artifacts.go — a genuine same-package file split (mirroring
#              prompt_memory.go's precedent), not new CLI surface — raising the count
#              15 → 16, which met but exhausted the prior budget's headroom. max_files
#              moved 16 → 17 (actual 16 + headroom 1, same convention as above) so the
#              next legitimate split has room rather than tripping immediately.
#              RECALIBRATED 2026-07-24: spine chaining (--chain) added 3 files
#              (chain.go + report.go + executor.go) — genuine function-level splits
#              from over-500-line engine_build.go/main.go, not new surface. max_files
#              17 → 21 (actual 20 + headroom 1).
#              RECALIBRATED 2026-07-25: forge trace command added trace_cmd.go.
#              max_files 21 → 23 (actual 22 + headroom 1).
#              RECALIBRATED 2026-07-27: durable chain resume/status plus the
#              fail-closed release executor, minimal prompt, output/receipt
#              contracts, and portable link validation raised cmd/forge to 30
#              cohesive command-boundary files. Platform pin helpers were merged
#              without weakening their build tags; max_files is 31 (actual 30 +
#              headroom 1). Moving the whole boundary to internal/release is
#              deferred until a second consumer justifies a cross-package API.
#              RECALIBRATED again 2026-07-27: the dynamic writes_adr pre/post tree
#              contract cannot fit artifact_provenance.go under the 500-line gate
#              and has a distinct responsibility, raising actual 30 → 31.
#              max_files 31 → 32 preserves the established one-file headroom.
#              RECALIBRATED 2026-08-10: ADR-0047 adds one universal CognitiveAtom
#              projection CLI and one adversarial contract test at the harness root;
#              projection/fixture logic is already split into the cohesive
#              harness/cognitive_atom_contract package. The measured global ceiling
#              is now harness at 33 files, so max_files 32 → 34 preserves exactly
#              one file of headroom without folding the independent contract into a
#              generic checker or weakening any other architecture threshold.
#              RECALIBRATED 2026-08-10: ADR-0048 adds one universal Artifact→
#              Evidence adapter CLI and one adversarial contract test at the
#              harness root; implementation logic remains in the cohesive
#              harness/artifact_evidence_adapter package. The measured ceiling is
#              now harness at 34 non-test files (the architecture metric excludes
#              tests), so max_files 34 → 35 preserves the same
#              one-file headroom without weakening another threshold.
#              RECALIBRATED 2026-08-10: ADR-0049 adds one universal command-
#              observation → Evidence adapter CLI at the harness root; the
#              implementation and source-adapter governance checks remain split
#              into cohesive sub-packages, while tests stay outside this metric.
#              The measured harness ceiling is now 35 non-test files, so
#              max_files 35 → 36 preserves exactly one file of headroom.
# 2026-08-10: ADR-0050 adds the pure Evolve repository-locator → Evidence
#              adapter CLI at the harness root; implementation remains inside
#              harness/evolve_repo_locator_evidence_adapter. The measured
#              harness ceiling is now 36 non-test files, so max_files 36 → 37
#              preserves exactly one file of headroom.
# 2026-08-11: ADR-0055 adds one universal ContextPackage contract CLI while
#              assembler/codec logic stays in a cohesive sub-package. The
#              measured harness ceiling is now 37 non-test files, so
#              max_files 37 → 38 preserves exactly one file of headroom.
# 2026-08-11: ADR-0056 adds one universal CapabilityGrant contract-only CLI;
#              its codec/evaluator and the extracted context-route floors stay
#              in cohesive sub-packages. The measured harness ceiling becomes
#              39 non-test files, so max_files 38 → 40 preserves the existing
#              convention of exactly one file of headroom. Further growth still
#              requires extraction or an evidence-backed recalibration.
# 2026-08-11: ADR-0059 adds one universal ApprovalRecord contract-only CLI;
#              codec/evaluator logic and the Agent Engineering reference data
#              remain in cohesive sub-packages. The measured harness ceiling is
#              now 40 non-test files, so max_files 40 → 41 preserves exactly
#              one file of headroom.
# 2026-08-12: Candidate ADR-0061 adds one universal KnowledgeUpdateProposal
#              contract-only CLI; codec/evaluator logic remains in its cohesive
#              sub-package. The architecture scanner measures harness at 42
#              non-test files, so max_files 42 → 43 preserves exactly one file
#              of headroom without changing a runtime or authority boundary.
# 2026-08-12: ADR-0062's checker and ADR-0063's extracted workflow-verdict
#              checker bring the measured harness root to 44 non-test files.
#              Both implementations remain in cohesive sub-packages/modules;
#              max_files 43 → 45 preserves exactly one file of headroom.
# 2026-08-12: ADR-0065 adds the universal GraphSnapshot checker while projector
#              logic remains in its cohesive sub-package. The architecture scanner
#              measures harness at 45 files; max_files 45 → 46 keeps headroom 1.
# 2026-08-14: ADR-0077 adds one universal WorkIntent candidate checker while
#              semantic and governance logic remain in cohesive sub-packages.
#              The scanner measures harness at 47 non-test files; 46 → 48
#              preserves exactly one file of headroom.
# 2026-08-14: ADR-0079 adds one universal authenticated ADR approval candidate
#              checker while the structural implementation remains in its
#              cohesive sub-package. The scanner measures harness at 48
#              non-test files; 48 → 49 preserves one file of headroom.
# 2026-08-14: ADR-0086 and Proposed ADR-0088 each add one universal pure
#              contract checker at the harness root; their validator, fixture,
#              and graph logic remain in cohesive sub-packages. The scanner
#              measures harness at 51 non-test files, so max_files 50 → 52
#              preserves exactly one file of headroom.
# 2026-08-15: ADR-0089 adds one cohesive governance integration module while
#              distributing the already-counted ADR-0088 checker. The scanner
#              measures harness at 52 non-test files, so max_files 52 → 53
#              preserves exactly one file of headroom.
# 2026-08-15: Proposed ADR-0090 adds one universal pure Kernel-decision
#              contract checker while its validators and graph logic remain in
#              a cohesive sub-package. The scanner's current exact closure is
#              52 non-test files, so retaining max_files 53 preserves one-file
#              headroom.
# 2026-08-18: Proposed ADR-0091 adds one cohesive Kernel-decision governance
#              integration module while distributing the already-counted
#              ADR-0090 checker. The scanner measures harness at 53 non-test
#              files, so max_files 53 → 54 preserves one-file headroom.
# 2026-08-20: Proposed ADR-0093 adds one cohesive Decision Capsule governance
#              integration module and three scaffold-owned source distribution
#              files. The scanner measures harness at 55 non-test files, so
#              max_files 54 → 56 preserves one-file headroom.
# 2026-08-21: independent review hardening separates descriptor-pinned parent
#              boundaries, added-file reservations, and scaffold-ledger
#              reservations into three focused transaction modules. The scanner
#              measures harness/scaffold at 58 non-test files, so max_files
#              56 → 59 preserves the established one-file headroom.
#   fanin:     max 37 PRODUCTION importing files (forge-core/internal/asset);
#              tests excluded. RECALIBRATED 2026-08-12: strict reviewer/output-binding
#              runtime seams add small focused cmd files around the intentional innermost
#              workflow carrier; 37 → 39 preserves exactly two files of headroom.
#              RECALIBRATED 2026-07-29:
#              Rust `crate/self/super` imports are same-crate cohesion, not inbound
#              consumers; external Cargo-crate imports remain counted.
#   cognitive: 4 top-level source modules
#              (examples, forge-core, forge-runtime, harness)
#   functions: longest declared function <= 50 lines (policies.yml max_function_lines)
#   cycles:    0 circular module imports (policies.yml circular_dependency_count)
#
# DRIFT-GUARD: file.max_lines and root.max_files MUST equal harness/policies.yml.
# arch-check.mjs verifies this equality every run, so the two sources of truth
# cannot silently diverge. See `必须与 harness/policies.yml 一致` markers below.
# 2026-08-21: second-review hardening extracts durable transaction/stage authority
#              into one focused scaffold transaction module. The scanner measures
#              harness/scaffold at 59 non-test files, so max_files 59 → 60 keeps
#              the established one-file headroom.

# ── Repository root budget ────────────────────────────────────────────────
root:
  max_dirs: 10        # actual 4 source dirs + headroom
  max_files: 15       # 必须与 harness/policies.yml 一致 (max_root_files) — arch-check enforces equality

# ── Per-package / per-directory budget ────────────────────────────────────
package:
  max_files: 60       # actual 59 non-test harness/scaffold files + headroom 1
  max_exports: 30     # actual 28 (orchestrator and mode) + headroom — recalibrated 2026-07-27

# ── Per-file budget ───────────────────────────────────────────────────────
file:
  max_lines: 500      # 必须与 harness/policies.yml 一致 (max_file_lines) — arch-check enforces equality

# ── Fan-in budget (how many PRODUCTION files import one target) ───────────
fanin:
  max_importers: 39   # actual 37 (Go internal/asset); tests excluded — intentional innermost workflow carrier with two files of headroom.

# ── Cognitive-load budget (top-level source modules to hold in your head) ──
cognitive:
  max_root_modules: 8 # actual 4 (examples, forge-core, forge-runtime, harness) + headroom

# ── Architecture: layered dependency direction (THE headline rule) ────────
# Clean Architecture: dependencies point INWARD. Outer layers may depend on
# inner ones; inner layers MUST NOT depend on outer ones.
#   interfaces (outermost) -> application -> domain (innermost)
#   infrastructure is an outer detail: it may be wired in by interfaces, but
#   domain/application must never import it.
architecture:
  # Layers ordered OUTER → INNER for documentation. The checker deliberately
  # enforces the explicit forbidden edges below; it does not infer policy from
  # this order. Infrastructure is an outer adapter wired by interfaces.
  layers:
    - interfaces
    - infrastructure
    - application
    - domain
  # Map concrete on-disk directory segment names onto canonical layers.
  # A file's layer = the layer of the FIRST path segment that matches an alias.
  dir_aliases:
    domain: domain
    application: application
    service: application
    interface: interfaces
    interfaces: interfaces
    httpapi: interfaces
    store: infrastructure
    infrastructure: infrastructure
  # Forbidden layer imports, as "from -> to". A non-test source file whose layer
  # is <from> must never import a file/package whose layer is <to>.
  # Encodes: inner layers never import outer layers.
  forbidden:
    # domain (innermost) must import NOTHING outer/sibling-detail:
    - "domain -> application"
    - "domain -> interfaces"
    - "domain -> infrastructure"
    # application owns use-cases and ports; it must not import either outer
    # delivery interfaces or concrete infrastructure adapters:
    - "application -> interfaces"
    - "application -> infrastructure"
    # infrastructure adapters may implement domain/application ports, but must
    # not reach sideways into a delivery interface:
    - "infrastructure -> interfaces"

  # Scope note: layering applies only to directories mapped by dir_aliases.
  # All 38 production Rust files are mapped. The legacy Go core uses package,
  # fan-in, cycle and naming checks but is not yet fully classified into these
  # four canonical layers; this heuristic is not claimed as whole-repo proof.

# ── Naming: technical-role directory names that signal "organized by tech
# detail, not architecture" (cognitive-architecture skill, principle 1/2). A dir
# with one of these basenames is flagged UNLESS it is blessed as a real layer in
# architecture.dir_aliases (e.g. `service` -> application). A CHEAP PROXY: it
# catches obvious technical-layering grab-bags; it cannot prove a structure is
# good (that stays the architect's judgment + the 30-second tree litmus).
naming:
  anti_patterns:
    - utils
    - util
    - common
    - commons
    - misc
    - helpers
    - helper
    - manager
    - managers
    - handler
    - handlers
    - impl
